# Generated data and charts -- these get rebuilt by the scripts, so we
# don't clutter the repo's history with large CSV/PNG files. Only the
# CODE that produces them needs to be version-controlled.
data/*.csv
charts/*.png

# Common Python junk files
__pycache__/
*.pyc
.DS_Store
.venv/
