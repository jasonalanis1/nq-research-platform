[pytest]
# tests live in tests/ only; _to_delete/ holds files the device shell cannot unlink
# (retired code moved aside by src/git_unlock.sh-style moves) and must never be collected
testpaths = tests
norecursedirs = _to_delete .git data research docs __pycache__
