# Session — 11:00 pm cycle (2026-09-17)

**S004 was stress-tested before it reached paper, and it is a KILL.** The screen's own-drift
baseline gate stripped 80% of a +76.01 pt/trade headline and passed the remaining +15.2433
pt/trade. That remainder does not survive overlap correction, exposure matching, or the
exposure-matched stability check. S004 never entered PAPER, so no paper record exists and none
was adjusted; its mandatory s.7 Salvage is owed and blocked.

## WHAT NEW INFORMATION THIS RUN PRODUCED
The H118 lineage's fourth large clean positive number is the index going up plus bought
volatility. S004's margin over its own-drift baseline is +15.2433 pt/trade with a calendar-time
block-bootstrap CI90 of −0.66…+32.84 points (R: −0.0198…+0.0447, P(margin ≤ 0) = 0.257), and
the sign FLIPS when the overlapping 10-session holds are thinned to non-overlapping ones
(−12.22 pt and −0.34 pt on two thinnings). The effective independent sample is ~56 trades, not
554. Of the +76.01 gross headline: ~80% index drift, ~12% the filter selecting 5.8% more
volatile sessions, ~8% a residual that straddles zero. 28 of 554 trades carry 240% of the
effect. A new failure mode is named: **overlap confusion** — a correct baseline measured on a
sample far smaller than its trade count — the companion to failure mode 7. Two of
`integrity_checks.py`'s greens are shown to be weaker than they look, one of them
arithmetically incapable of firing.

## Step 1 — Preflight, and the six steps the gate blocks
`python3 src/cycle_preflight.py --owner cycle --note "11:00 pm cycle"` — lock held, budget 105
min, sweep written, both daily checkers ok. Receipt `research/_cycle_preflight.json`.

REFERENCE-DATA GATE (session 2026-09-17), unchanged and not worked around — STALE: CPI
(2023-12-12), FOMC (2021-09-22), NFP (2023-12-08), VXN (2026-09-02):

| blocked step | blocked by |
|---|---|
| `step3_paper_scoring` | VXN, FOMC, CPI, NFP |
| `step3_b2_risk_state_decision` | VXN, FOMC, CPI, NFP |
| `step4_salvage_check` | VXN, FOMC, CPI, NFP |
| `salvage_reruns` (S007, S009 — now **S004** too) | VXN, FOMC, CPI, NFP |
| `step4_specify_S009a` | FOMC, CPI, NFP |
| `step4_specify_S010a` | VXN |

Still runs and did: sourcing, SPECIFY/FREEZE/SCREEN of price-only candidates, close-out, and
the measurement work below — none of which reads a stale series.
`research/infrastructure/reference-data-currency-2026-09-16.md`'s two cron lines from Jason's
own Terminal remain the only fix; cboe/fed/bls/fred are 403 from both shells.

## Step 2 — Data check
`data/NQ_1min_databento_2026-09-16.csv`, price data through 2026-09-16, one session behind and
inside the normal 7 am CT top-up cadence. No new bars since the 9:00 pm cycle. Not an interrupt.

## Step 3 — Advance the paper book: BLOCKED, nothing scored
`step3_paper_scoring` is blocked by VXN, FOMC, CPI and NFP. **Nothing was scored and nothing was
booked.** Blocking series named above. The book is reported as of the last clean measurement
(`src/cycle_preflight.py` PAPER BOOK):

| strategy | trades | days | status |
|---|---|---|---|
| S008 | 2 | 9 | the only live 6-week clock — 38 trades to judgment, six-week mark in 4.7 wk |
| S001 | 1 | 9 | SLOW — background, no queue slot, judged at 40 |
| S002 | 2 | 9 | SLOW |
| S001a | 0 | 9 | SLOW |
| S003 | 0 | 9 | SLOW |

Nothing is at a judgment point; nothing is judged on fewer than 40 trades (Amendment 1).

## Step 4 — S004 stress-tested, and killed

Full write-up: `research/studies/S004-scrutiny-2026-09-17.md`. Tool:
`src/study_s004_scrutiny.py`. Numbers: `data/s004_scrutiny.json`.

**Measurement only.** The frozen spec (`research/infrastructure/strategy-specs/S004-vwap-dist-low-drift-vs-own-drift.md`,
sha256 8408305460…) and module (`src/strategy_s004_vwap_dist_low_drift.py`, sha256 f8f8832579…)
were not edited, and the screen was **not** re-run with different parameters (directive s.13).
Everything below is computed from the frozen `data/screen_S004_with_baseline.json`. Arm A (554
trades) is a strict subset of arm B (1,663) and every shared trade agrees to 1e-6
(`src/study_s004_scrutiny.py:load_table`), so this is **one trade and one selection rule**.

### 1. Overlap correction — nothing clears zero
House convention: `src/gate_conditions_hyp162.py:calendar_block_bootstrap` resamples blocks of
consecutive SESSIONS and re-derives the split inside each resample;
`src/baseline_relative.py:block_bootstrap_diff_ci` uses block = the overlap length. 3,000
resamples, seed 7, 90% intervals.

| unit | block | margin | CI90 | clears zero | P(≤0) |
|---|---|---|---|---|---|
| points | 10 | +15.2432 | −0.6636 … +32.8398 | no | 0.057 |
| points | 20 | +15.2432 | −1.0066 … +32.3218 | no | 0.057 |
| % notional | 10 | +0.1809 | −0.0197 … +0.3925 | no | 0.073 |
| R | 10 | +0.0117 | −0.0198 … +0.0447 | no | **0.257** |
| R | 20 | +0.0117 | −0.0215 … +0.0442 | no | 0.278 |

Non-overlapping thinning (holds ≥ 10 sessions apart, iid bootstrap then legitimate):
offset 0 → n=140, **−12.2230 pt** / −0.0368 R, CI90 −68.78…+44.19; offset 5 → n=138,
**−0.3438 pt** / +0.0019 R, CI90 −56.49…+54.30. **The sign flips.** Effective independent
sample ≈ **56**, not 554.

### 2. Beta / exposure matching — 61% of the margin is bought volatility
Holding time (10 RTH sessions) and size (1 micro) are identical in both arms by construction, so
the only exposure the filter can add is volatility and index level. It adds volatility:
atr14/entry **1.740%** on selected sessions vs **1.645%** on all; stop rate **13.54%** vs
**10.94%**; entry price 7,405.85 vs 7,470.13 (it picks slightly *cheaper* sessions, so notional
matching costs little). Volatility-matched: **+0.0117 R ≈ +5.9 pt** at the sample's 508-pt mean
risk, i.e. ~9.3 pt of the +15.24 is exposure. Of the +76.01 headline: **~80% index drift, ~12%
volatility selection, ~8% residual whose interval straddles zero.**

### 3. Subperiod stability — the points margin tracks the index level
Each subperiod against its **own** contemporaneous baseline:

| year | n sig/all | points | % notional | R |
|---|---|---|---|---|
| 2015 | 76/212 | +8.378 | +0.2081 | +0.0196 |
| 2016 | 85/254 | −0.303 | +0.0030 | −0.0010 |
| 2017 | 63/253 | +19.621 | +0.3073 | +0.0950 |
| 2018 | 103/254 | +8.978 | +0.1418 | −0.0091 |
| 2019 | 81/254 | +19.029 | +0.2514 | +0.0406 |
| 2020 | 88/255 | +39.737 | +0.4273 | +0.0412 |
| 2021 | 58/181 | +39.292 | +0.3261 | +0.0121 |
| first half | 266/831 | +6.369 | +0.1175 | +0.0058 |
| second half | 288/832 | +21.059 | +0.2238 | +0.0166 |

6/7 years positive in points, but the points column rises with the index level. In the
exposure-matched R unit: 5/7 positive, negative in 2016 and 2018, and **2017 alone is 48% of the
seven-year R total** (+0.0950 of +0.1984). Weak, not absent — and it does not rescue §1.

### 4. The project's own placebo suite — which ran, which did not
`src/integrity_checks.py:run_suite()` on the 554 signal trades in points, against the **correct
null** (the own-drift baseline 60.7698, not zero), with the full 1,663-session population and
the selector mask supplied so the placebo leg could actually run. **No check in this suite reads
VXN or the macro calendar, so none of the seven was gated by the reference-data block** — what
the gate blocks is the s.7 Salvage menu (conditions 1 and 4), handled below. 7 attempted: **4
green, 2 RED, 1 NOT APPLICABLE**.

- **RED — 3. cost_sensitivity:** CI90 53.09…98.62 at 2× costs crosses the 60.77 null. The mean
  moves 0.007 pt; the red is the *width*, which is §1 arriving through another door.
- **RED — 6. concentration:** top 5% = 28 of 554 trades carry **240%** of the effect. Per the
  check's own docstring a share above 100% is the stronger form of the defect.
- **NOT APPLICABLE — 2. overnight_intraday_split:** a 10-session hold has no leg decomposition.
  **Not a pass.**
- GREEN — 1. drift_null (the screen's gate restated, not new evidence); 4.
  roll_date_contamination (2.2% vs a 3.0% base rate — genuinely informative); 5.
  subperiod_stability (**but its internals say the opposite**: against a fixed null the first
  half of trades averages 37.93 pt and the second 114.10 vs 60.77, i.e. −22.8 then +53.3; it
  greens only because a fixed null is contaminated by the index rising); 7. placebo (**and the
  inverted leg is an arithmetic identity** for a difference of means — −15.2433 × 554/1109 =
  −7.6147 exactly, one hundredth of a point under the threshold, incapable of firing when the
  signal is a minority; the shifted t+1 leg reaches 41% of the real effect while re-selecting
  31% of the same sessions).

### Disposition — KILL, recorded with the numbers
Registry `research/ledger/strategies.jsonl` (`src/strategy_registry.py:169` append), stage
**KILL**, verdict and every number above on the row. S004 **never entered PAPER**: no paper
record exists and none was adjusted. Deregistered from
`src/bot_stack_paper_run.py:234` (`register_strategy("s004", …)` commented out with the reason,
so no paper row can ever be written for it); the frozen module it imported is untouched and
every other test in `tests/test_strategy_s004.py` still runs against it.

**The mandatory s.7 Salvage is OWED and BLOCKED**, not skipped and not run partially. S004 is
added to `src/rerun_salvages.py`'s owed list beside S007 and S009: `SALVAGE_INPUTS["S004"]`
(with `trades_path` because its screen file carries two arms) and `owed()` now returns a KILL
whose first salvage never ran, as well as a SUPERSEDED row awaiting its rerun — both are owed
for the same reason and refused for the same reason, so they go through the same door. Verified:
`rerun_salvages.owed()` → `['S007', 'S009', 'S004']`. Four new tests in
`tests/test_rerun_salvages.py`.

**One thing Jason should know, and it was NOT acted on.** S004's Discovery trades end
2021-09-15, so `rerun_salvages.coverage_check("S004")` reports every menu series covering every
one of its trade dates — by that module's own test its salvage could run today. The block comes
from the standing **session-level** gate (`step4_salvage_check`, stale VXN and calendar as of
the current session date), which is Jason's rule and not Tony's to reinterpret. The salvage waits
for the cron lines like everything else.

## Step 4 (second item) — S005: read, scoped, NOT advanced
`research/studies/hyp162-portfolio-2026-09-14.md` was read first, as it must be: hyp-000162 is
**VALIDATED_NOT_PROMOTED** because the 10:00 ET opening-range update carries no measurable
information about the midday range beyond the pre-open stack (joint retention 17.7% linear /
52.0% stratified, lower bounds −14.3% / +14.9% against a 50% bar; MAE 0.3252 → 0.3256, i.e. the
forecast gets *slightly worse*). So S005's honest question is narrow: does using it to set stop
distance or target on an **already-frozen** module improve that module's screen result? That is
a SPECIFY + FREEZE + SCREEN of a new overlay spec against a named host module (S008 is the
obvious host — the only non-SLOW strategy in the book), and it was **not** started this cycle:
Job A consumed the budget, and a half-built overlay is worse than none. S005 stays at SOURCE at
the head of the queue with the question and the host named. **No stage was claimed for it.**

## Step 5 — Close-out
- `python3 -m pytest -q` → **704 passed**. `tests/test_strategy_s004.py`'s registration test was
  rewritten to assert the deregistration and the KILL (a test file, not a frozen artifact); four
  tests added to `tests/test_rerun_salvages.py`.
- `ops_checks.py` WARN ×3, all pre-existing: the 9:00 pm report's missing Agents section, two
  reports omitting the new-information line, and the six standing items awaiting Jason.
- `pipeline_sweep.py --write`; `cycle_close.py --label "11:00 pm cycle"`; `session_report.py`.
- **Triggers are NOT booked from this session** — no scheduling tool is available here. The
  orchestrating session still owes tomorrow's bookings.

## Agents
- **Statistical — RAN.** The whole of §1–§4: block bootstraps, the non-overlapping thinning, the
  exposure decomposition, the subperiod table and the integrity suite.
- **Integrity Gate — RAN.** Held the frozen spec, module and screen output unedited; confirmed
  arm A ⊂ arm B trade-for-trade; confirmed no paper record existed to adjust; refused to
  reinterpret the session-level reference-data gate in S004's favour even though the module's
  own coverage test would have allowed the salvage.
- **Director — RAN.** Issued the KILL with the numbers; ordered the salvage owed rather than
  skipped; declined to start S005 on the remaining budget.
- **LEARN — RAN.** `research/KNOWLEDGE.md`: new dated section plus failure mode 9, *overlap
  confusion*.
- **Operations Manager — RAN.** Preflight, data check, gate reporting, close-out.
- **Discovery — NOT RUN.** No new candidate sourced; the queue is not empty (S005, S006).
- **Mechanism / Monetization — NOT RUN.** No SPECIFY this cycle.
- **Portfolio — NOT RUN.** Dormant until two strategies hold KEEP (s.4). Its hyp-000162 finding
  was *read* for S005's scoping, which is not the same as running it.
- **Bot (B7) — NOT RUN.** Paper scoring blocked by the reference-data gate.
