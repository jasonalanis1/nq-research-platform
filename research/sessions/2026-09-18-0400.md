# 11:00 pm cycle — September 17th 2026 (session date 2026-09-18 UTC)

Scheduled, unattended. Standing directive `research/infrastructure/standing-directive-2026-09-15.md`
+ Amendment 1 + Amendment 2. Steps 1–5 of s.3. Jason was not contacted.
HEAD at start 2b1eaa1, tree clean. Commits this cycle: d4c2aaf (FREEZE), 27036f8 (SCREEN),
ddccc30 (NEXT_UP), plus the close-out compliance rows.

## 1. Preflight — `research/_cycle_preflight.json`

`python3 src/cycle_preflight.py --note "11:00 pm cycle"`. Lock taken, budget 105 min, pipeline
sweep written, both daily checkers ok.

**REFERENCE-DATA GATE — 6 steps BLOCKED** by CPI (ends 2023-12-12), FOMC (2021-09-22),
NFP (2023-12-08), VXN (2026-09-02) — `research/ledger/data_coverage.json`,
`src/cycle_preflight.py:reference_data_gate()`:

| blocked step | blocked by | why |
|---|---|---|
| `step3_paper_scoring` | VXN, FOMC, CPI, NFP | every paper row goes through B2 — `src/risk_state_engine.py:decision_for` |
| `step3_b2_risk_state_decision` | VXN, FOMC, CPI, NFP | `src/risk_state_engine.py:require_reference_data` refuses the session |
| `step4_salvage_check` | VXN, FOMC, CPI, NFP | s.7 menu conditions 1 and 4 — `src/salvage_check.py:vxn_labels` / `news_labels` |
| `salvage_reruns` | VXN, FOMC, CPI, NFP | `src/rerun_salvages.py` — S007, S009, S004 owed |
| `step4_specify_S009a` | FOMC, CPI, NFP | S009a trades only on days with no scheduled macro event |
| `step4_specify_S010a` | VXN | S010a selects LOW-VXN sessions only |

**Nothing was bypassed, nothing defaulted.** Blocked is not aborted: Step 4 ran in full on
price-only data.

## 2. Data check — 47.51 hours stale at close-out, the 48-hour line NOT crossed

Newest file `data/NQ_1min_databento_2026-09-16.csv`, last bar **2026-09-16 00:50 ET**.
Recomputed at run time, twice:

- at the data check, **2026-09-18 00:13 ET → 47.40 h**
- at close-out, **2026-09-18 00:20 ET → 47.51 h**

**48 h is NOT reached, so NO directive s.10 reason-3 condition is declared this cycle.**
It crosses at **2026-09-18 00:50 ET / 04:50 UTC** — about half an hour after this cycle closed.
**The next cycle will measure ≥48 h and must declare it.** Root cause is known and logged and is
**not a code defect**: the 7 am cron's Databento fetch dies `403 Forbidden` at the egress proxy,
the same wall as the VXN and macro feeds. It needs Jason.

## 3. Paper book — BLOCKED, nothing scored, no fill invented

`step3_paper_scoring` is gated (section 1). No `bot_stack_paper_run.py` run, no verdict, no
`paper_book.py` scoring. Book unchanged from preflight: **S008** 2 trades / 10 days, 38 to
judgment, six-week mark in 4.6 wk — the only live clock; **SLOW, background, no queue slot**:
S001 (1 trade), S002 (2), S003 (0), S001a (0). Nothing is at 40 trades, so no strategy was owed
a verdict under Amendment 1.

## 4. Step 4 — S011 advanced three stages: SPECIFY → FREEZE → SCREEN

### 4a. What was inherited

`research/mechanisms/volume-conditioned-daily-reversal-m18.md:140` — Scan 026 closed hyp-000152
**P1_PASS_P2_FAIL**: cell 1 (HIGH-volume sign-adjusted next-session return) n=555, mean −0.0942,
ci_90 (−0.1523, −0.0463) — credibly negative; cell 4 (HIGH − LOW) mean −0.0590, ci_90
(−0.1253, **+0.0054**) — not credible; cell 5 (unconditional) mean **−0.0425**; kill check
first-minute share 0.036 against a 0.5 threshold, so it is a session-long effect and not a gap
artifact. `research/ledger/closures.jsonl` (hyp-000152) records `not_retest:` **"Volume as a
gradient on daily reversal."**

### 4b. The one judgement call, made explicitly

The candidate was sourced as "daily reversal **after a high-volume session**". **It was not built
that way.** The closure forbids the volume gradient by name, `research/NEXT_UP.md:117` says the
spec may not reintroduce a volume tercile, and the HIGH cell's −0.0942 is not credibly different
from the unconditional −0.0425 — picking it because it is the bigger number is selecting on a
difference the data refused to support. So the traded claim is **cell 5**, and the expected edge
used at the gate is the honest **0.0425 × ATR14**, not 0.0942 × ATR14. `vol_ratio` is recorded in
every signal's `market_context` and is read by no decision
(`src/strategy_s011_daily_reversal_vs_own_drift.py:213`; test
`tests/test_strategy_s011.py:test_volume_is_recorded_but_never_decides`). Precedent: S003, a raw
effect closed at the hypothesis stage and built as a trade for the first time.

### 4c. SPECIFY — `research/infrastructure/strategy-specs/S011-daily-reversal-vs-own-drift.md`

Whole trade: signal session P = any completed RTH session; `M(P) = Close(last RTH bar) −
Open(09:30)`; skip `M == 0`; direction = **fade** `sign(M(P))`; entry = **open of the 09:30 bar of
the next RTH session**; stop = `entry ∓ 2.0 × atr14` (lagged) as a **disaster cap**; **no target**
(unreachable sentinel); flat at **15:55 the same session** — no cross-session hold, no `exit_ts`;
1 micro MNQ; costs from `src/cost_model.py`, all four combinations (spec s.4–s.5).

**Mechanism, and its weakness stated honestly** (spec s.3): the forced counterparty is the
liquidity provider left holding unchosen overnight inventory taken on at a price concession, who
unwinds it against the prior session's direction through the next session — the 0.036 first-minute
share is that footprint. **It is weak, and the spec says so**: the mechanism's own size prediction
*failed*. Inventory concession should scale with flow size; volume, the best available proxy, does
not grade it. What survives may be index-level negative lag-1 autocorrelation with no identifiable
forced seller. That is why the own-drift baseline is a **gate** and not a diagnostic.

**PRE-FREEZE EDGE-vs-COST CHECK** (Amendment 2, `src/cost_model.py:223 specify_gate`): expected
gross edge `0.0425 × atr14`; Discovery-era atr14 ≈ 99.8 pt median / 133.3 pt mean, taken from the
554 already-published S004 screen trades (`data/screen_S004_with_baseline.json`,
`risk_points / 4.0`) → **4.24 pt/trade against the 1.300 pt MNQ market round trip: PASSES**
(`passes: true`, verdict `SCREEN`). It would pass at atr14 = 31 pt. **Cost was never the binding
question for this candidate; the baseline was.**

**Where this should fail** — the S011 Salvage menu, pre-registered in spec s.7 so it cannot be
chosen after results: (1) high-VXN sessions; (2) trend vs range, the Llorente informed-move
confound and the likeliest way this loses money; (3) time of day — **degenerate**, the entry clock
is fixed at 09:30, reported as not applicable rather than silently dropped; (4) scheduled-news
sessions; plus era decay across Discovery halves and the up/down direction split.

### 4d. FREEZE — commit d4c2aaf, before any outcome data

`research/ledger/strategies.jsonl` FREEZE row: spec sha256
`b286e28d70995aa8...`, module `src/strategy_s011_daily_reversal_vs_own_drift.py` sha256
`973916b7092c414e...`. Baseline runner `src/screen_s011_own_drift_baseline.py` (sha256 recorded,
**not** frozen, **not** a strategy). Unit tests on synthetic bars only,
`tests/test_strategy_s011.py` — 8 passing, written and committed with the freeze.

### 4e. SCREEN — Discovery only, all four combinations, with the baseline gate

`python3 src/screen_s011_own_drift_baseline.py` → `data/screen_S011_with_baseline.json`.
2101 sessions, 1669 trades, 0 open/unresolved, win rate 0.5021, gross **5.6544 pt/trade**.

| combination | net $ | net R | avg R | gross pt/tr | cost pt/tr | net pt/tr | |
|---|---|---|---|---|---|---|---|
| **MNQ market (DECISION BASIS)** | **14,535.10** | 15.5 | 0.0093 | 5.6544 | 1.300 | **4.3544** | MAKES MONEY |
| MNQ limit (**OPTIMISTIC**) | 15,369.60 | 18.0 | 0.0108 | 5.6544 | 1.050 | 4.6044 | MAKES MONEY |
| NQ market | 163,876.90 | 21.0 | 0.0126 | 5.6544 | 0.745 | 4.9094 | MAKES MONEY |
| NQ limit (**OPTIMISTIC**) | 172,221.90 | 23.5 | 0.0141 | 5.6544 | 0.495 | 5.1594 | MAKES MONEY |

**Arm B, the own-drift baseline** (identical trade forced LONG on the identical 1669 sessions —
NQ's own next-session drift): net **$4,844.88**, **1.4514** net pt/trade, avg R net +0.0037.

**THE GATE, pre-registered in the frozen spec s.6 before the screen ran:**
1. net > 0 → **$14,535.06** ✓
2. margin over own drift → **4.3544 − 1.4514 = +2.9030 net pt/trade** (+0.0056 R/trade) ✓

The pre-freeze falsifier — *"fails if MNQ-market net ≤ $0.00 or the margin ≤ 0.0000 net pt/trade"*
— **did not fire**. **VERDICT: PAPER.**

### 4f. The S004 lesson, applied BEFORE the claim

**Effective sample = 1669, equal to the trade count.** 1669 trades, 1669 distinct sessions, 0
duplicates, 0 holds crossing their own session; the hold is 09:30 → 15:55 of one session so no two
holding windows share a bar. The one dependence left is a **one-lag chain in the signal** (trade
t's signal is the session trade t−1 was entered in), not in the outcome. Reported, not hidden.

Four honest qualifications, disclosed **with** the pass and recorded in the SCREEN ledger row:

1. **Concentration is high** — the top 5% of trades (83) carry $35,382 = **243% of net**; median
   trade **+$1.40**, win rate 50.2%. The average is not the typical trade. This is the same flag
   that went RED on S006 (top 5% at 188%), and it is inherent to an unstopped open-to-close hold.
2. **The margin is carried entirely by the short leg.** On the 743 post-DOWN sessions arms A and B
   are the same long trade and differ by **$0.00 by construction**; the paired A−B difference is
   positive on only **26.4%** of trades with a median of $0.00. The whole **+$9,690** margin comes
   from the 926 post-UP sessions, where being short beat being long by $10.46/trade. The surviving
   claim is narrower than "the fade beats the drift": it is **"after an UP session, being short
   beats being long."**
3. **Era tilt inside Discovery** — half 1 +$1,917.67, half 2 +$12,617.39. Both positive, **no
   subperiod sign flip**, but the later half carries 6.6×.
4. **avg R is tiny (+0.0093) by construction** — R = 2.0 × atr14 is a wide disaster cap, and
   1649 of 1669 trades exit on the 15:55 clock (7 stops, 13 session-end fallbacks). Dollars are
   the meaningful unit here; both are reported.

None of these is a gate under the directive, and none was used to re-cut the strategy after seeing
results.

### 4g. Disposition — registered, then parked

**SLOW projection from its own screen: 1669 / 2101 = 0.7944 trades/session × 126 = 100.1 projected
six-month trades against 40 → NOT SLOW**, ordinary six-week clock (verified, as asked; under
1.00/session because sessions with `M(P) == 0`, no 09:30 bar or no atr14 window are skipped).

S011 is registered and marked **BLOCKED_PENDING_REFERENCE_DATA** for paper entry. It was **not**
added to `bot_stack_paper_run.STRATEGIES`: `step3_paper_scoring` and `step3_b2_risk_state_decision`
are gated, so a strategy entered now would book nothing and record
`blocked_by_stale_reference_data`. Forcing it in would be a defaulted value in all but name. It
takes **no queue slot** while blocked, beside S009a and S010a, and enters PAPER on the first cycle
with current coverage. Its salvage is likewise blocked (menu conditions 1 and 4 need the stale
series) — but no salvage is owed, because it **passed**.

**S012** (hyp-000158, monthly opex-week delta-hedge unwind, M25) registered at SOURCE so the queue
does not sit empty. It is price + clock only — the third Friday is computable — so it is not
gate-blocked; expect SLOW (~12 events/yr). Then hyp-000147 (intraday periodicity, M17).

## 5. Close-out

`python3 -m pytest -q` → **712 passed**. `python3 src/cycle_close.py --label "11:00 pm cycle"` →
**CLOSE COMPLETE — every mechanical check passed** (preflight clean, tests, ops, git committed and
pushed, lock FREE, moved: 1 stage change recorded — in fact four registry stage events).
`git status --short` empty, `origin/main == HEAD`. Session report written last with path:line
citations, the PAPER BOOK section and the blocked-steps report.

**Not done, and deliberately:** tomorrow's cycle triggers were **not** booked — this session has no
scheduling tool. That is handed back for booking.
