# 9:00 pm cycle — September 17th 2026 (session date 2026-09-18 UTC)

**Movement: S006 (Stack A) closed SOURCE → LEARN without spending a freeze or a
screen, and S011 sourced in its place. The queue is blocked, not exhausted.**

## 1. Preflight — `research/_cycle_preflight.json`

Lock held, budget 105 min, pipeline sweep written, both daily checkers ran.

**REFERENCE-DATA GATE (session 2026-09-18) — 6 steps BLOCKED**, stale series CPI,
FOMC, NFP, VXN. Never bypassed.

| blocked step | blocked by |
|---|---|
| `salvage_reruns` (S007, S009 owed) | VXN, FOMC, CPI, NFP |
| `step3_b2_risk_state_decision` | VXN, FOMC, CPI, NFP |
| `step3_paper_scoring` | VXN, FOMC, CPI, NFP |
| `step4_salvage_check` | VXN, FOMC, CPI, NFP |
| `step4_specify_S009a` | FOMC, CPI, NFP |
| `step4_specify_S010a` | VXN |

Coverage: VXN ends 2026-09-02 (14d short, 8 consumers, `src/data_topup_vxn.py`);
FOMC 2021-09-22, CPI 2023-12-12, NFP 2023-12-08
(`src/data_topup_macro_calendar.py`). S009a and S010a remain
BLOCKED_PENDING_REFERENCE_DATA and take no queue slot.

## 2. Data check — 45.31 hours stale, 48h NOT yet crossed, ~2.7 hours to go

Newest file `data/NQ_1min_databento_2026-09-16.csv`, last bar
**2026-09-16 00:50:00-04:00**. Recomputed at run time (2026-09-17 22:08 ET /
2026-09-18 02:08 UTC): **45.31 hours stale**. The directive's 48-hour line
(s.10 reason 3) is crossed at **2026-09-18 00:50 ET (04:50 UTC)** — **2.69 hours
after this cycle's data check**. So this cycle does **not** declare a Section 10
reason-3 condition; it declares that the condition is **imminent**. The 11:00 pm
cycle will likely span the crossing and the 1:00 am cycle is unambiguously past
it; whichever cycle first measures ≥48h declares reason 3.

Root cause is known, logged and **not a code defect**: the 7:00 am cron's
Databento fetch dies with `403 Forbidden` at the egress proxy — the same wall
that keeps VXN and the macro calendar stale. It needs Jason. Nothing in this
cycle can clear it.

## 3. Paper book — BLOCKED, nothing scored

`step3_paper_scoring` is blocked by the gate (every paper row goes through B2,
`src/risk_state_engine.py:decision_for`, which reads `vxn_level_vs_trailing`).
No row was written and no fill was invented. Standing: S008 2 trades / 10 days
(38 to judgment, six-week mark in 4.6 wk) is the only live clock; S001 (1),
S002 (2), S003 (0), S001a (0) are SLOW background. S004 KILLED, S005 LEARN.

## 4. S006 (Stack A) — closed to LEARN, no freeze, no screen

Write-up: `research/studies/S006-stack-a-not-rebuilt-2026-09-17.md`.
Ledger row: `research/ledger/strategies.jsonl` (S006, stage LEARN).

The honest first question was "is there anything here to build?", and the record
answers it three times over:

1. **The question is already answered, with power.**
   `research/mechanisms/stack-a-b2-context-x-b3-breakout.md` is PRE-REGISTERED and
   carries a U8-1.0 closure form: IN_CONTEXT − OUT_CONTEXT = **+0.0247R, 90% CI
   (−0.0496, +0.0976)**, Šidák-adjusted at N=460 (−0.1401, +0.1896); cells n=566
   and n=977 against a floor of 100; **clean null, powered**, mechanism
   **falsified**. The design excluded any difference above ~+0.19R where the
   mechanism implied ~+0.23R.
2. **The closure form names the forbidden retest verbatim** — "expected-range
   tercile crossed with the opening-range break, in any variant of tercile edges,
   breakout windows or targets. That is the same question." S006 as sourced **is**
   that variant. The one permitted retest (a *different*, non-volatility context
   variable with its own mechanism) is not met.
3. **The trigger loses money unconditionally.**
   `research/integrity/mechanical-stack-a-2026-09-14.json`: `raw_mean = −0.026599`
   over n=1543 **before costs**. Under the Amendment 2 cost model the 1.300 pt MNQ
   round trip is subtracted from an already-negative mean — no cost combination
   screens positive, so a screen would have been spent confirming arithmetic. Its
   integrity profile is the S004 failure twice over: **2 RED of 7** —
   `5. subperiod_stability` (first half −0.0603 credible, second half +0.0070 not
   credible, **signs disagree**) and `6. concentration` (top 5% carry **188%** of
   the effect); `2. overnight_intraday_split` and `7. placebo` **did not run**.

Directive s.9 lists "Stack A" at the top of the revamp list, but that listing
predates the September 14th closure. s.9's own bottom rule — *clean nulls stay
dead* — governs. The revamp list is for near-misses with a **real** mechanism, not
for reanimating a powered null. No salvage is owed: a null is not a screen loss.
Capacity action carried forward: **reduce**.

**No SLOW projection is reported, because nothing was specified, frozen or
screened. That is the outcome, and it cost one cycle and zero research capital.**

## 4b. Queue assessment — blockage, NOT directive s.10 reason 2

With S006 at LEARN the *unblocked* queue is empty, but this is **not** exhaustion:
the revamp list (`research/ledger/revamp_list.json`, 27 "top" entries) still holds
unworked NQ candidates with named forced counterparties. Sourced this cycle so the
next cycle opens with a queue rather than an interrupt:

- **S011 — daily reversal after a high-volume session, NQ (hyp-000152 / M18),
  measured against NQ's own next-session drift.** hyp-000152 closed
  **P1_PASS_P2_FAIL**: the HIGH-volume sign-adjusted next-session return *was*
  credibly negative on Discovery (ci_90 **−0.1523, −0.0463**) and it closed only
  because volume added **no gradient** (diff ci_90 −0.1253, +0.0054). The
  conditioning layer is dead; the effect under it is a credible cell that was never
  built into a trade and never costed — the exact profile s.9 reserves the top of
  the list for, and the opposite of S006. Forced counterparty named and not
  optional (Campbell–Grossman–Wang 1993): liquidity providers holding overnight
  inventory they did not choose. **Price and volume only → not gate-blocked**;
  fires most sessions → should project non-SLOW (Amendment 1's SOURCE preference).
  Binding question at SCREEN is **cost**: a 0.05–0.15 ATR effect against a 1.300 pt
  MNQ round trip may well die there, which is a cheap and honest answer. The spec
  may **not** reintroduce a volume tercile to improve the number.
- Then **hyp-000158** (monthly opex-week delta-hedge unwind, NQ / M25) — real
  forced dealer flow, calendar computable from the third Friday, not from the stale
  macro feed; expect SLOW (~12 events/yr).
- Then **hyp-000147** (intraday periodicity, same slot next day / M17) — intraday,
  fires most days, mechanism doc names a flow.

Deliberately **not** queued: **hyp-000139** (overnight/RTH divergence reversion) —
its own mechanism doc records the counterparty as "weak/possibly none, mechanical
mean-reversion of a stretched statistic". That fails the Mechanism Gate, and this
cycle is not in the business of reanimating statistics without counterparties.

## 5. Close-out

`python3 -m pytest -q` → **704 passed**. `src/cycle_close.py` run to
CLOSE COMPLETE; commits pushed, `origin/main == HEAD`. Three commits:
S006 closure, S011 sourcing + NEXT_UP, this session file.
