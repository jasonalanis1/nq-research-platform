# 7:00 pm cycle — September 17th 2026 (session date 2026-09-18 UTC)

**Movement: S005 taken SOURCE → SPECIFY → FREEZE → SCREEN → LEARN in one cycle.
The result is a NULL and the null is the point.**

## 0. Operational: five cycles lost to a bridge outage today

The **9:00 am, 11:00 am, 1:00 pm, 3:00 pm and 5:00 pm** cycles were all missed or
aborted — the remote bridge to Jason's Mac was down or dropping for most of the
day. This is an **operational fact worth logging, not a failure of the chain**:
the directive, the queue and the ledger were all intact and this cycle resumed
them without loss. Recorded so the day's compliance row is honest about why
`research/_cycle_compliance.jsonl` is thin.

**The leftover preflight receipt.** `research/_cycle_preflight.json` was sitting
modified-uncommitted at HEAD `675519f`, residue from the 1:00 pm cycle whose
preflight call timed out mid-execution. Checked before doing anything with it: it
is a **complete, valid receipt** — `ran_at` 2026-09-17T18:08:49Z, `ok: true`,
`failed: []`, all ten steps present and each `ok`, and it correctly carries the
post-kill state (`queue.salvage_owed: ["S004"]`, and S004 moved into the gate's
`blocked_candidates`). It was therefore **committed, not discarded**
(`cf9884b`), with the honest note that the cycle which produced it opened and
then aborted to the outage. Discarding a valid receipt would have erased the
evidence that the 1:00 pm cycle ran its opening sequence at all.

## 1. Preflight — `research/_cycle_preflight.json`

Lock held, budget 105 min, pipeline sweep written, both daily checkers ran
(H118: no new signal, 2 open; EXP047 snapshot saved).

**REFERENCE-DATA GATE (session 2026-09-18) — 6 steps BLOCKED**, stale series
CPI, FOMC, NFP, VXN. Never bypassed.

| blocked step | blocked by |
|---|---|
| `salvage_reruns` (S007, S009 owed) | VXN, FOMC, CPI, NFP |
| `step3_b2_risk_state_decision` | VXN, FOMC, CPI, NFP |
| `step3_paper_scoring` | VXN, FOMC, CPI, NFP |
| `step4_salvage_check` | VXN, FOMC, CPI, NFP |
| `step4_specify_S009a` | FOMC, CPI, NFP |
| `step4_specify_S010a` | VXN |

Coverage: VXN ends 2026-09-02 (15d short, 8 consumers, `src/data_topup_vxn.py`);
FOMC 2021-09-22, CPI 2023-12-12, NFP 2023-12-08
(`src/data_topup_macro_calendar.py`). Still awaiting the two cron lines from
Jason's own Terminal; neither shell can reach the sources. **Blocked ≠ aborted**
— everything else in the cycle ran.

## 2. Data check — price data is 43 hours stale and crosses 48 in under five hours

Newest file `data/NQ_1min_databento_2026-09-16.csv`, **last bar
2026-09-16 00:50:00-04:00**. At the time of this cycle (2026-09-17 20:01 ET)
that is **43.2 hours stale**. It crosses the directive's **48-hour line at about
2026-09-18 00:50 ET**, i.e. within roughly five hours, during the 9:00 pm or
11:00 pm cycle. **Not yet across it, and flagged now so the crossing is not a
surprise** — see section 10 below.

**`data/_fetch_run.log` — and the cause is not what was assumed.** The log does
*not* show a sleeping Mac. It shows the fetch **running and failing at the
network layer**:

```
requests.exceptions.ProxyError: HTTPSConnectionPool(host='hist.databento.com', port=443):
  Max retries exceeded ... (Caused by ProxyError('Unable to connect to proxy',
  OSError('Tunnel connection failed: 403 Forbidden')))
RuntimeError: Fetch FAILED on the 2015 chunk (2015-01-01 to 2016-01-01) ...
```

`hist.databento.com` is **not on the egress allow-list** of the shell the cron
runs in, so the tunnel is refused 403 before any data moves. The script's own
chunk cache is intact and it will skip cached years and resume at 2015 once the
host is reachable. **This is the same class of problem as the VXN/macro staleness
and has the same fix: it must be run from Jason's own Terminal, or the host
allow-listed.** Worth stating plainly because "the Mac was asleep" would have
been the wrong diagnosis and the wrong fix.

## 3. Paper book — BLOCKED, nothing scored

`step3_paper_scoring` is blocked by the gate (every paper row goes through B2,
`src/risk_state_engine.py:decision_for`, which reads `vxn_level_vs_trailing` and
vetoes on a scheduled-event day). **No strategy was scored and no fill, exit or
outcome was booked.** Book unchanged: S008 2 trades / 10 days (38 to judgment,
six-week mark in 4.6 wk, the only live clock); SLOW and taking no queue slot —
S001 (1), S002 (2), S001a (0), S003 (0). S004 KILLED. Nothing at 40 trades, so
no verdict was due.

## 4. Step 4 — S005 advanced four stages to a null

Full write-up: `research/studies/S005-or-width-stop-overlay-2026-09-17.md`.
Spec `research/infrastructure/strategy-specs/S005-or-width-stop-overlay-on-S008.md`
(sha256 `a89e3c3b4b56892d…`), module
`src/strategy_s005_or_width_stop_overlay.py` (sha256 `332420d32acb3caa…`),
frozen in its own commit `9206c06` **before any outcome data was touched**.

**The question, narrowly.** hyp-000162 (opening-range width → midday range) is
VALIDATED_NOT_PROMOTED — `research/studies/hyp162-portfolio-2026-09-14.md`, joint
residual retention ~17.7% against a 50% bar, and adding it to the forecast stack
made the forecast slightly *worse* (MAE 0.3252 → 0.3256). So: never a standalone
strategy. The only question left was whether it improves an **already-frozen**
strategy when used to set **stop distance and target** — sizing is a non-linear
transform of a forecast, so redundancy for forecasting does not imply
uselessness for sizing.

**Host: S008, and why.** Best-performing frozen strategy and **the only one in
PAPER that is not SLOW** (0.3351 trades/session → 42.2 projected in six months
vs 40), so a real improvement would reach a 40-trade judgment on a working
clock. Mechanically right, too: S008 already sizes its stop off an intraday
**range** (`0.50 × the 09:30–15:00 range`) — the exact quantity this fact would
inform. S002 is higher-frequency but is SLOW *and* is an overnight carry with no
intraday-range risk for the fact to touch. **S008's frozen spec and module were
not edited** (s.13); the overlay is its own frozen spec and module and imports
the host read-only.

**Overlay:** `risk' = clip(ORW / trailing-20 median ORW, 0.75, 1.35) × 0.50 ×
RNG`, target 1.5R on the rescaled risk, k = 1.0 exactly when the input is
missing. ORW known at 10:00 ET, five hours before the host's 15:00 decision.
Pre-freeze Amendment 2 gate applied to the overlaid strategy as a whole: host
grosses 2.6851 pt/trade against the 1.300 pt MNQ market cost, overlay rescales
risk by at most ±35% without changing entries → passes, goes to SCREEN.

**SCREEN, Discovery slice only (2101 sessions), all four cost combinations,
net $ at one contract** (`data/screen_S005.json` vs
`data/screen_S008_host_alone.json`; both also in the SCREEN registry row):

| combination | host alone | with overlay | delta | net R host → overlay |
|---|---:|---:|---:|---:|
| **MNQ market — DECISION BASIS** | **1,950.18** | **1,901.21** | **−48.97** | −8.2 → −7.9 |
| MNQ limit — **OPTIMISTIC** | 2,302.18 | 2,253.21 | −48.97 | −3.6 → −3.5 |
| NQ market | 27,316.22 | 26,826.48 | −489.74 | +1.9 → +2.0 |
| NQ limit — **OPTIMISTIC** | 30,836.22 | 30,346.48 | −489.74 | +6.4 → +6.5 |

Gross 2.6503 vs 2.6851 pt/trade against the 1.300 pt MNQ market cost. Both limit
columns are an **OPTIMISTIC UPPER BOUND** — a resting limit order is assumed
always to fill at its price; real ones miss fills and are adversely selected.

**704 trades in both. All 704 entries identical. Win rate 0.4688 in both. Only
16 of 704 (2.3%) changed exit reason.** The scaler was live, not degenerate:
k mean 1.1099, median 1.1657, range 0.750–1.350, 362/704 (51.4%) at a clip
bound, zero fallbacks to k = 1. A hair better in R, a hair worse in dollars —
neither material.

**Why — both mechanisms were named in the spec before the screen.** (2) Scaling
stop and 1.5R target *together* by the same k is a **no-op in R by
construction**; it can only act through which level is touched first, and that
channel moved 2.3% of trades to a wash. (1) ORW and RNG are both range measures
of the **same session**, so the scaler largely re-states a quantity the host's
stop already used — the Portfolio finding arriving from a different direction,
now on a *traded* result rather than a forecast.

**Disposition: S005 → LEARN**, per the rule fixed in the spec *before* the
screen. Not salvaged (it did not lose money, so none is owed) and **not
varied** — hunting a k-band or tercile split that looks better is exactly the
ex-post slicing the salvage rule forbids. **This null is a good result**: it
closes a question the project has carried since the Portfolio stage.
hyp-000162 has now failed as a forecast input *and* as a sizing input, by two
methods on two slices, and should be treated as **closed** unless a genuinely
different use appears. Lessons written to `research/KNOWLEDGE.md`.

**Queue advances to S006 (Stack A).**

## 5. Close-out

`python3 -m pytest -q` → **704 passed**, 38 warnings, 57.19s.
Commits: `cf9884b` (1pm receipt), `60342ee` (preflight), `9206c06` (freeze),
`4579cae` (screen + null). Pushed incrementally throughout, given the day's
bridge instability; `origin/main == HEAD` verified after each push.
`data/screen_S005.json` and `data/screen_S008_host_alone.json` are gitignored by
standing repo convention — their numbers live in the SCREEN registry row, which
is committed.

## 10. Section 10 — reason 3 watch, price data

**Not yet an interrupt, but about to be.** Price data is 43.2 hours stale and
crosses the directive's 48-hour line at approximately **2026-09-18 00:50 ET**.
The cause is now known precisely and is **not** a sleeping machine: the
Databento host is refused by the egress proxy (403) from both shells. Jason
already owes two cron lines from his own Terminal for VXN and the macro
calendar; **this is a third item on the same errand and the same fix**. Flagged
upward this cycle so the crossing is reported before it happens rather than
after.
