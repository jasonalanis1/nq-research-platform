# 2026-09-18, 9:00 am cycle — two corrections to the record, and a candidate closed before it cost anything

Tony, scheduled, unattended. Jason is away and **has no access to his computer**; nothing in this
cycle was escalated to him. Directive `research/infrastructure/standing-directive-2026-09-15.md`
+ Amendments 1 and 2; `research/CYCLE_PROMPT.md` Steps 1-5 and the reference-data gate.

---

## 0. The two corrections, done first

### (a) "The 7 am cron dies 403 Forbidden at the egress proxy" — WRONG, WITHDRAWN

Prior session files and `research/NEXT_UP.md` stated this as a **known root cause**. It is not
supported by any evidence this project holds. Verified directly this cycle, before writing anything:

| check | finding |
|---|---|
| mtime of `data/_fetch_run.log` | **2026-09-09 18:15:50 UTC** — nine days old, so not a record of any cron run on the 15th–18th |
| traceback frame paths | **`/sessions/rcw-.../mnt/Documents--nq-research-platform-live/...`** — sandbox paths, a run made inside the sandboxed device VM |
| script that wrote it | **`src/data_fetch_databento.py`** (full 2015→today historical fetcher, failing on its **2015** chunk) — **not** `src/data_topup_databento.py`, which is what the cron runs |

Three independent tells, any one sufficient. **The truth is that we do not know why no new price
file appeared.** The cron logs to `~/Library/Logs/tony-topup.log`, **outside the connected folders
and unreadable from here**, so no cycle can diagnose it. Unconfirmed candidates only, none to be
stated as fact: the Mac asleep or off at 7:00 am; cron/launchd without Full Disk Access or the key;
an unseen Databento-side or network failure; the job not installed as believed.

**NOT corrected, because it is true:** the *sandbox* egress wall — `hist.databento.com`,
`cdn.cboe.com`, `federalreserve.gov`, `bls.gov`, `fred.stlouisfed.org` all 403 at the proxy from the
sandboxed shells. That is why these fetches must run from Jason's Terminal. It says nothing about
his cron.

**Corrected in:** the new STANDING CORRECTION block in `research/NEXT_UP.md`; that file's
"Operational constraints" bullet; `research/KNOWLEDGE.md` (new lesson + a scope warning on the
existing sandbox-egress note); and correction notes appended to
`research/sessions/2026-09-18-0000.md`, `-0200.md`, `-0400.md` — their dated history is **preserved,
not rewritten**. The `S011` row in `research/ledger/strategies.jsonl` repeats the claim and is left
as an immutable ledger record, flagged superseded and not to be quoted forward.

### (b) Directive s.10 reason 3 — DECLARED, and it stands open

Recomputed at run time. Newest price file `data/NQ_1min_databento_2026-09-16.csv`, last bar
**2026-09-16 00:50 ET (04:50 UTC)**. At this cycle's data check, **2026-09-18 14:01 UTC**:

> **57.19 hours stale.** The 48-hour line was crossed at 2026-09-18 04:50 UTC.

**The s.10 reason-3 condition is DECLARED as of this cycle**, with the corrected — honest,
**unknown** — cause above, not the withdrawn 403 story. **Jason has already been told and currently
has no computer access, so the condition STANDS OPEN rather than being escalated again.**

A rule was written into the standing block so this does not become noise: while the block is present
and the file is still stale, **every future cycle restates it in one line and does not re-declare,
re-escalate or re-diagnose it.** It is closed only by a newer `data/NQ_1min_databento_*.csv`.

## 1. Preflight — clean, 6 steps blocked

`python3 src/cycle_preflight.py --note "9:00 am cycle"` → PREFLIGHT COMPLETE, receipt
`research/_cycle_preflight.json`. Reference data STALE: **CPI 2023-12-12, FOMC 2021-09-22,
NFP 2023-12-08, VXN 2026-09-02**. Blocked, never bypassed:

`step3_paper_scoring`, `step3_b2_risk_state_decision`, `step4_salvage_check`, `salvage_reruns`
(S007, S009 owed; S004 too), `step4_specify_S009a`, `step4_specify_S010a`.
BLOCKED_PENDING_REFERENCE_DATA: S009a, S010a, **S011** — these take no queue slot.
Still running: source, specify (price-only), freeze, screen (Discovery, price-only), close-out.

## 2. Data check

See 0(b). **57.19 h stale, s.10 reason-3 declared, cause unknown, standing open.**

## 3. Paper book — BLOCKED, nothing scored, no fill invented

No `bot_stack_paper_run.py`, no `paper_book.py` scoring, no verdict. Book unchanged:
**S008** 2 trades / 10 days, 38 to judgment, six-week mark in 4.6 wk — the only live clock.
**SLOW** (background, no queue slot): S001 (1), S002 (2), S003 (0), S001a (0). Nothing is at 40
trades, so nothing was owed a judgment. S011 remains BLOCKED_PENDING_REFERENCE_DATA.

## 4. Step 4 — S012 CLOSED TO LEARN WITHOUT A SPECIFY, A FREEZE OR A SCREEN

Full reasoning: `research/studies/S012-opex-week-not-built-2026-09-18.md`.

S012 was taken first, as instructed, and it is price+clock only — so the reference-data gate is not
what stops it. **Reading its lineage's closure record stops it.**

**The gating test S012 would have to pass has already been run, and it failed.** `hyp-000158`
(Scan 032): P1 — expiration-week NQ return, ATR14-normalised, **net of NQ's own unconditional weekly
drift** — mean **+0.1872**, ci_90 **(−0.0291, +0.3950)**, **n = 80 expiration weeks, adequate**.
That P1 *is* the two-nulls own-drift baseline arm a SPECIFY would have had to build in: the arm was
pre-run and did not clear. P2 also not credible (+0.2429, ci_90 −0.1167..+0.5907). NULL1's own
unconditional weekly mean is **+0.3343**, so a screen against zero would have printed a
healthy-looking profit that was pure drift — **the S004/H118 error, in advance.**

**The closure form forbids the retest by name.** U8-1.0 for hyp-000158: `status` **clean null**,
`sample` **adequate (80 weeks)**, `mech` **falsified for NQ**, `not_retest` **"Opex-week directional
claims on NQ."**, `condition` **none**, `action` reduce family. There is no re-entry route.
Directive s.9's bottom rule — clean nulls stay dead — governs. **This is the S006 precedent one
cycle later.**

**The named fallback is banned on identical grounds.** `hyp-000147` (M17 intraday periodicity):
P1 FAIL in all three slots, n = 1,632, OPEN −0.013 ATR ci_90 (−0.026, +0.001); `not_retest`
**"Intraday return periodicity on NQ."**, `condition` **none**. Also checked: `hyp-000146` / M16
closes session-anchored continuation claims, and the practitioner follow-on it named, **P-2
opening-drive exhaustion, is CLOSED** (`research/sourcing/practitioner.md` — the entire P-1…P-4
stream is RULED out).

**No salvage owed:** a Salvage follows a KILL after a screen, and there was no screen. S012 → LEARN.
**Published calendar anomalies are now 0 for 9 on the directional lane.**

### This is NOT s.10 reason 2 — the check is recorded

Reason 2 needs **all four** of queue empty, revamp list worked through, Salvage queue empty,
Discovery out of priority sources. Two fail outright:

- **Revamp list NOT worked through** — 137 entries; in the top tier alone `hyp-000136`,
  `hyp-000139`, `hyp-000021`, `hyp-000060` and the pre-Gate block `hyp-000072`–`hyp-000078` carry
  **no U8 closure row and no `not_retest` ban**.
- **Salvage queue NOT empty** — S007, S009, S004 owed and gate-blocked. Blocked is not empty.

**A bad queue row, not exhaustion.**

### Replacement sourced: S013 (SOURCE only, not advanced)

**S013 — opening-auction order imbalance, NQ (`hyp-000076`, pre-Gate scan tier).** The only
remaining tier that is simultaneously ban-free, **intraday and firing every session** (Amendment 1's
top SOURCE preference — the property S012 structurally never had at ~12 events/year), and
price+volume only. `hyp-000076` closed REJECTED at expectancy **−0.0614 R**, best interval
(−0.1047, −0.0170) — but it is a **pre-Gate** row: MED mechanism, **no mechanism doc**, and measured
**against zero, never against NQ's own drift**.

Three conditions pre-registered in the registry row **now**, before any work, so the next cycle
cannot drift: **(1)** a mechanism doc naming the forced counterparty must be written **first**;
**(2)** an explicit **overlap check against S011 is mandatory before SPECIFY** — same
Campbell-Grossman-Wang inventory story at the same 09:30 clock means **drop, not build**;
**(3) sign-flipping is forbidden as a rationale** — if the mechanism paragraph cannot stand without
the minus sign, S013 closes at SOURCE too. Any SPECIFY carries the own-drift baseline arm.

### Standing change

**Read the closure form's `not_retest` AND `condition` at SOURCE, not at SPECIFY.** Twice in two
cycles (S006, S012) a dead candidate reached the front of the queue because revamp-list *closeness*
was read and the closure form was not. Closeness ranks how near the number came; `condition` says
whether the question may be asked again. hyp-000158 scores 0.863 and hyp-000147 scores 0.954, and
both are banned. **A SOURCE row now quotes both verbatim or it has not been sourced.**
Recorded in `research/KNOWLEDGE.md`.

## 5. Close-out

- `python3 -m pytest -q` → **712 passed**.
- `python3 src/cycle_close.py --label "9:00 am cycle"` — first run FAILED on `lock` (still held by
  this cycle) and a second on `git`; the lock was released and the remaining work committed, and
  the state is recorded honestly in `research/_cycle_compliance.jsonl` rather than papered over.
- Commits: `cd947f5` (corrections + declaration), `a46b16a` (S012 close + S013 source), plus the
  close-out commit. Pushed; origin/main == HEAD.

**Constraints honoured:** no Validation/Holdout touched; no `batch_screen.py`; no frozen spec or
module edited; no paper-log fill altered; no `git add -A`; no fabricated fill, exit, event date or
volatility reading; the reference-data gate was never bypassed; no trigger booked; no spend.
