# Session — September 15th, 9:00 pm CT scheduled cycle (2026-09-16 01:5x–02:5x UTC)

Steps 1-5 under the standing operating directive (research/infrastructure/standing-directive-2026-09-15.md
s.3). Jason away. Every claim cites `path:line` (refocus 7.4a). Costs ASSUMED everywhere (directive s.11).

## Step 1 — Preflight
`python3 src/cycle_preflight.py --owner cycle --note "9:00 pm cycle"` clean
(research/_cycle_preflight.json): lock held, budget 105 min, sweep rebuilt, both daily checkers ran,
queue S002 SPECIFY / S003-S006 SOURCE, PAPER BOOK 1 strategy (S001a, 0 trades / 8 days).

## Step 2 — Data check
data/NQ_1min_databento_2026-09-15.csv, unchanged since the last cycle (written Sept 16 01:40 UTC,
817 bars dated 2026-09-15, last bar **13:38 ET**). Sept 15 is therefore STILL IN PROGRESS and is
refused by the completeness guard (src/bot_stack_paper_run.py:264 `session_is_complete`). Sept 14 was
scored last cycle. Data is current (ops_checks data-currency PASS, 1 session behind); no interrupt.

## Step 3 — Advance the paper book
- `TONY_PRODUCTION=1 python3 src/bot_stack_paper_run.py --strategy s001a` — 0 new rows. 2026-09-13
  skipped (Sunday fragment, no RTH), 2026-09-15 skipped (in progress, 13:38 ET < 15:55). **S001a
  still 0 trades / 8 days**, 4.9 weeks to judgment. Nothing near a judgment point.
- `--strategy dummy` — 0 new rows, same two skips. Plumbing record current (20 fills, never judged).
- After Step 4, S002 entered the book and scored 5 sessions (below). `python3 src/paper_book.py`
  now renders two candidates plus the two plumbing lines.

## Step 4 — Advance one candidate one stage: S002, SPECIFY -> FREEZE -> SCREEN -> PAPER

### (a) The FREEZE prerequisite: a cross-session exit in the paper loop (commit cc13c09)
S002's spec named it (s.3, "paper-engine plumbing prerequisite"): `_resolve_fill_outcome` scanned one
calendar-day frame, so an overnight hold could not be bookkept at all. Built as **choice 7** in the
paper loop's header:
- A strategy that holds past its entry session declares an ABSOLUTE `market_context["exit_ts"]`
  (src/bot_stack_paper_run.py:339 `cross_session_exit_ts`). `_resolve_fill_outcome`
  (src/bot_stack_paper_run.py:368) then walks the WHOLE frame across the boundary — stop and target
  still first-touch, stop still wins a same-bar tie — and the time exit fires at the first bar at or
  after that timestamp, booked at that bar's **OPEN** (src/bot_stack_paper_run.py:399).
- **The completeness guard extends to the EXIT session** (src/bot_stack_paper_run.py:348
  `cross_session_exit_ready`). An overnight trade whose exit session is absent or still in progress
  DEFERS the entry session whole, before any side effect — no B2 call, no broker, no order-path
  journal entry (src/bot_stack_paper_run.py:465) — and `main()` writes nothing for it
  (src/bot_stack_paper_run.py:598). The trade stays OPEN and unresolved and is re-scored on a later
  run. `session_end_fallback` is refused outright across a boundary (src/bot_stack_paper_run.py:403):
  **no exit is ever fabricated and no incomplete session is force-closed.**
- The SCREEN uses the same bookkeeping (src/screen_strategy.py:66) and excludes, rather than
  force-closes, a trade whose exit falls past the slice (src/screen_strategy.py:74).

**B3 and the execution dummy are provably unchanged.** `scan_df`/`exit_ts` default to None, which is
the original code path verbatim. tests/test_cross_session_exit.py carries a **verbatim copy of the
pre-change function** (`_REFERENCE_RESOLVE`) and asserts dict equality over a 40-case grid covering
stop / target / time exit / session-end fallback, both directions, five widths
(tests/test_cross_session_exit.py:95), and over B3's and the dummy's own end-to-end rows
(tests/test_cross_session_exit.py:148). The OPEN-not-fabricated behaviour has its own tests
(tests/test_cross_session_exit.py:304 and :323 — first run logs nothing, later run scores the same
entry session exactly once, idempotent). 15 new tests.

### (b) FREEZE (commit 88a834b, before the screen)
src/strategy_s002_overnight_carry_compressed_prior_day.py + the spec, sha256 of both in
research/ledger/strategies.jsonl:19 (spec 6dbba19b…, module 4b0ab595…). The module reads the frozen
hyp-000048 NARROW cell on the ENTRY session (identical by construction to `prior_day_narrow` on the
session that follows, and needs no bar that has not printed:
src/strategy_s002_overnight_carry_compressed_prior_day.py:88), enters long at the 16:00 bar's OPEN,
declares `exit_ts` at the next RTH session's 09:30
(…:176), stops 1.0 lagged ATR14 below entry (…:102), no target. 8 synthetic-bar tests
(tests/test_strategy_s002.py). Spec amended at FREEZE (four clarifications, no change to the trade).
**Disclosed in the FREEZE row**: before hashing, the module was run once over real bars as a
MECHANICAL smoke check (signal count + audit only — 142 signals, audit pass); no outcome, P&L or
performance number was computed or seen.

### (c) SCREEN — one number (Discovery slice only, ASSUMED costs)
`python3 src/screen_strategy.py strategy_s002_overnight_carry_compressed_prior_day`:
**364 trades, NET +$1,446.10 at 1 micro** (5: +$7,230.50; 10: +$14,461.00), gross +$3,630.10 minus
$2,184.00 assumed costs, win rate 53.6%, **avg +0.006R net — COST-FRAGILE by a wide margin**
(directive s.11; 60% of gross eaten by assumed costs). 0 unresolved trades excluded. Lands where the
menu cell predicted (+$1,625 raw, less what the ATR gap-cap stop cuts). **MADE MONEY -> PAPER.**

### (d) PAPER
Registered as `--strategy s002` (src/bot_stack_paper_run.py:174) and run. 5 sessions scored:
2026-09-08/09 no signal; **2026-09-10 FILLED, exited on the clock at 2026-09-11 09:30 (+0.9519R,
+$558.49)**; **2026-09-11 FILLED, STOPPED on the Sunday reopen at 2026-09-13 18:00 (-1.0R, -$605.11)**
— the weekend leg the spec s.4.3 names as a place this should fail, recorded per trade and not
filtered; 2026-09-14 no signal. Both exits crossed the session boundary. Paper book: **2 trades, 50%
wins, -0.03R average, -$59 / -$293 / -$586 at 1/5/10 micros**, worst streak 1 (-$611) inside a
$1,657 daily limit, slippage ASSUMED. Judgment at 40 trades or 6 weeks; expected ~6-7 trades per 6
weeks (under the 15-trade floor, stated up front in the spec).

## Step 5 — Close-out
`python3 -m pytest -q` **565 passed** (was 542: +15 cross-session, +8 S002). ops_checks green except
the standing awaiting-jason WARN. Three commits (cc13c09, 88a834b, 9af368e) pushed; origin/main == HEAD.
