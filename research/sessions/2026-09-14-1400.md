# Session — September 14th, 9:00–9:5X am CT (9:00 am scheduled cycle)

First cycle of the pre-booked, unattended day. Lock: owner=cycle. Budget: 105 min.
**First cycle to open with `cycle_preflight.py` and close with `cycle_close.py`.**

Note on the trigger: this cycle fired carrying the stale 1:00 am prompt, whose
work items (B7, U28, U6, the shelf draw) were all completed earlier today.
`research/NEXT_UP.md` is the authoritative handoff and was followed instead. The
stale prompt also instructs a rescheduling via `update_trigger`; that was
correctly NOT done — the day is pre-booked as independent one-shots and
re-chaining would duplicate.

## What moved

**Preflight — clean, and the first time the daily checkers ran inside the
opening sequence.** Lock, budget, pipeline sweep, H118 daily checker, EXP047
weekly checker, shelf position. All ok. The checkers had not run since the
11:00 pm cycle; they run now because the sequence is one command rather than a
protocol to remember.

**Ordering principle: no bot milestone can be advanced.** Stated rather than
skipped past, per the rule.
- **B7** is live but **data-blocked**. `bot_stack_paper_run.py` ran and correctly
  reported nothing new: price data still ends 2026-09-08, the anchor session, and
  that session is already logged. Its blocker is data currency, not code, and new
  data is a purchase — Jason's standing decision of September 12th is NO
  PURCHASE, so this cycle did not pull any.
- **B4b** remains blocked on the IBKR futures-permission cooldown (~October 13th).
  Not re-raised.
- **B8** is Jason's authorization alone.
So the cycle fell through to research, which is what the ordering principle
prescribes when nothing on the trunk can move.

**Statistical stage — hyp-000162 (M30 opening-range width → midday range).
PASS, and it survives the binding multiplicity correction.**

Construction reproduced from Scan 036 exactly; nothing redefined at this stage.
Full sample n = 555 HIGH / 556 LOW, difference **+0.5193**, ci_90 (+0.4475,
+0.5975).

| Question | Result | Pass |
|---|---|---|
| Q1 stability (chronological halves) | 1st +0.4676 [+0.3851,+0.5696] · 2nd +0.5794 [+0.4707,+0.6979] | PASS |
| Q2 regime (trailing-20d range median) | low-vol +0.4560 [+0.3600,+0.5337] · high-vol +0.6082 [+0.5047,+0.7308] | PASS |
| Q3 multiplicity (Šidák, N_discovery=467, BINDING) | raw [+0.4475,+0.5975] → adjusted **[+0.3511,+0.6875]** | PASS |
| Q4 selection sensitivity (reported) | 11:30–13:30 +0.5193 · 11:00–13:00 +0.4977 · 12:00–14:00 +0.5141 · 11:30–14:00 +0.5357, all credible | reported |
| Power at one-shot Validation | 700 sessions (~233/arm), **1.000** at the Discovery effect, **0.815** at half | — |

Write-up: `research/studies/hyp162-statistical-stage-2026-09-14.md`.

**The U6 placebo red was carried forward unchanged, deliberately.** Separating
"wide opening range" from "wide multi-day volatility regime" needs a new
conditioning variable (prior-day OPENING range) and therefore a new registered
trial. Absorbing that into this stage would be scope creep past a frozen spec,
and spending a trial on it is the Director's call. Q2 is the closest this stage
legitimately gets: the effect holds in both volatility regimes, which rules out
"only in turbulent weeks", but it is larger in the high-vol half (+0.61 vs
+0.46), which is consistent with the concern rather than against it.

**Sourcing — Entry 36 / M32, observatory channel.** Owed (shelf was at the
floor) and had been skipped for several cycles. `location_in_range` — where the
open sits inside the prior day's range, known at **09:30**, half an hour earlier
than M30's state — has never been the conditioning variable of a registered
scan. The queue footprint is monotone across every window (LOW above all-days
everywhere, HIGH below everywhere), so the direction is pre-registered rather
than fitted. Mechanism: volatility asymmetry — dealer hedging of short downside
convexity plus vol-control funds cutting mandated exposure, both concentrating
into the close, which is why the gating window is the last hour.

**Two gating controls pre-registered, one of them bought with today's mistakes.**
P3 residualizes on the overnight gap, because "opened low in yesterday's range"
is close to "gapped down" and volatility asymmetry after declines is already a
known effect. **P4 requires the shifted-signal placebo to fail to reproduce the
effect** — written in directly because hyp-000162 passed every registered
prediction this morning and then tripped exactly that check. Pre-registering it
means the answer arrives before the Gate instead of after. Shelf 3/3 → **4/3**.

### Agents
- Operations — RAN: preflight (all steps), sweep, both daily checkers, budget,
  full close-out via `cycle_close.py`.
- Statistical — RAN: hyp-000162's four questions, binding Šidák, power at the
  proposed Validation review point. Verdict PASS.
- Mechanism — RAN: M32 doc written BEFORE any scan, per the gate.
- Discovery — RAN in its sourcing capacity only (Entry 36). No scan registered,
  no hypothesis ID spent.
- Director — NOT RUN, and this is now the binding item: hyp-000162 and
  hyp-000156 both owe a capital call at the Validation gate.
- Integrity Gate — NOT RUN (blind Gate owed after the Director call). Its
  mechanical half ran on hyp-000162 yesterday and its red stands.
- Validation / Monetization / Portfolio / LEARN — NOT RUN. Nothing reached them.

### Stage coverage
Statistical (hyp-000162, PASS) → owed: Director capital call, then the blind
Gate. Mechanism written for M32. One hypothesis advanced a stage; no hypothesis
ID spent, no scan registered, no Holdout slot touched.

## What broke
Nothing in the machinery. The only friction was the stale trigger prompt
described in the header, which is a known loose end: rewriting it raises the
approval dialog Jason has banned, so it stays until he taps one through. It is
inert while this session holds context, and `NEXT_UP.md` is authoritative.

## WHAT NEW INFORMATION THIS RUN PRODUCED

**hyp-000162 is statistically solid — the doubt about it is now precisely
located, and it is not a statistical doubt.** The association is stable across
eras, holds in both volatility regimes, is insensitive to the midday window
boundary within ±0.04, and survives Šidák correction at 467 project-wide
Discovery trials with its whole interval above zero. Validation would be well
powered (82% even at half the Discovery effect), so a null there would be
informative rather than ambiguous. What remains unknown is not *whether* the
association is real but *what it is an association with* — and that question is
now written down in the form a Gate can actually answer.

**The project learned something from hyp-000162 within one cycle of it
happening.** M32's frozen scope contains a gating control (P4, the shifted-signal
placebo) that exists only because this morning's draw passed everything it was
asked and still turned out to have a problem nobody had thought to ask about.
That is the cheapest form of learning available — the cost was one candidate's
ambiguity, and the payment is that the next candidate cannot reach the Gate with
the same question open.

## Next
**The Director call is now the binding constraint**, on two candidates:
hyp-000162 (Statistical PASS, one integrity red, well-powered Validation
available) and hyp-000156 (Statistical PASS ×4, Validation blocked on a 6E data
pull parked on Jason). Nothing else can advance past it. After that, the blind
Integrity Gate for hyp-000162 — handed the specific question, not a general one.
Shelf is at 4/3 with Entry 32 the oldest drawable, so a draw is authorised if a
later cycle has room. B7 stays data-blocked until the price data moves.
