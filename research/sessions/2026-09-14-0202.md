# Session — September 13th, ~9:02–9:55 pm CT (scheduled cycle)

First cycle under the CONDITIONAL STACK format, adopted at the ~9:00 pm staff meeting.
Jason's direction going in: "Let's push it in now." So the stack build was the cycle's
primary work, ahead of the hyp-156 Gate conditions.

## What moved

**BOT:** nothing. B0–B3 remain DONE; **B4 (order path) is still blocked on Jason** — the
broker paper account (Tradovate) is his to create; the monthly spend is pre-approved, the
account is not. Nothing was signed up for, nothing was spent. Fell through to research, as
instructed.

**Stack format, built and used in one cycle (U25, U26, U27, U29):**
- `src/stack_spec.py` — the schema, the validator and the hash. Refuses out-of-time-order
  layers, refuses a third layer until the format has a result, refuses a spec with no
  per-layer mechanism, no interaction claim, no floor, or no minimum detectable effect.
- `project_wide_multiplicity.STACK_REGISTRY` + `register_stack()` + `stacks_attempted()` —
  a stack cannot run until its spec hash is registered; re-registering a changed spec under
  the same id is refused as a different trial.
- Mechanism template gains §4a (one mechanism per layer), §4b (interaction claim), §9a
  (floor + MDE before any scan). Closure form gains **UNDERPOWERED**, distinct from
  `data limitation`, consuming an attempt. Blind-packet brief gains the six stack checks.
- `src/stack_scan_runner.py` — one runner for every stack; layers in time order,
  year-chunked occurrences, paired mode, difference bootstrap (N_BOOT 20,000) and Šidák.
- **Stack A ran.** Registered as scan_035 with hash `4df7b408` before any data was touched.

## Stack A — the result (hyp-000161, REJECTED)

Question: does the expected-range regime (B2, known at the prior close) change what a B3
opening-range break *means*?

| cell | n | mean R |
|---|---|---|
| in context (top-tercile expected range) | 566 | −0.0109 |
| out of context | 977 | −0.0357 |
| unconditional reference — NOT evidence | 1,543 | −0.0266 |

Gating difference +0.0247R, CI 90% (−0.0496, +0.0976); Šidák-adjusted at N=460
(−0.1401, +0.1896). **Powered null.** Both cells cleared the floor of 100 by a wide margin;
the adjusted interval excludes anything above +0.19R, and the mechanism implied ≈0.23R.
Sign as predicted, magnitude not credible. Study: `research/studies/stack-a-2026-09-13.md`;
closure form in the mechanism doc §12 and `research/ledger/closures.jsonl`.

Consequence for the bot: **B2 is a sizing and permission layer, not an entry filter.** The
BASE vs BASE+B2 paper comparison stays on the roadmap but starts with the stated prior that
B2 does not change the entry's expectancy. The format now has one result, so the 2-layer
ceiling lifts to 3; one more powered null closes the format (rule 10).

## hyp-000156 — all five blind-Gate conditions resolved, in writing, before data

C1 one test: the **paired per-day diff**, the construction the candidate was promoted on;
baseline named honestly as the US-RTH hourly baseline, superseding the mechanism doc's
24-hour wording — before the test, not after. C2 `src/validate_hyp156.py` written and
hashed (`e679fba6…`), Šidák as a rule `max(20, N)`, N_BOOT 20,000. C3 gating window stays
03:00–04:00 ET with DST-aware diagnostics rather than a changed window; ATR warms up from
the Discovery tail; provenance/roll parity asserted, refuses on mismatch. C4 `data_discovered`
convention explained in the packet. C5 bar-density and first-5-minute variance-share added
as pre-registered non-gating diagnostics.

**Now unblocked for Jason:** the 6E Validation-slice data pull (2021-10-04 → 2024-01-03).
$5+ rule — his call, not a purchase this cycle.

## U24 — the mislabel can't happen again

`research_ledger.update_status()` now refuses VALIDATION CANDIDATE (and every later status)
unless a validation-slice row exists for that hypothesis, with the status vocabulary written
into the module. 5 tests.

### Agents
- Director — RAN (ordered the cycle: stack build first per Jason; ruled Stack A closed).
- LEARN — RAN (Stack A is new; no prior closure covers the interaction).
- Mechanism — RAN (Stack A doc written before the run, §4a/4b/9a filled).
- Discovery — RAN (scan_035, year-chunked, 1,543 occurrences).
- Statistical — RAN (paired difference, bootstrap, Šidák, power statement).
- Integrity Gate — NOT RUN as a fresh subagent: a powered null needs no adversarial review
  to protect against a false positive, and the Gate's stack checks were instead built into
  the packet brief this cycle. The hyp-156 conditions were written to the Gate's own list.
- Validation — NOT RUN (no candidate owed one; hyp-156 blocked on data).
- Monetization — NOT RUN (nothing passed).
- Portfolio — NOT RUN (no new fact).
- Operations — RAN (registry, ledger guard, tests, console, budget, close-out).

### Stage coverage
Sourcing (stack A from the roadmap, not the queue — stated) · Mechanism · Discovery ·
Statistical · closure. No Validation, no Holdout, no Monetization this cycle.

## What broke
1. `research_ledger` had no `read_all`; the new guard called it — fixed to `_read_all`.
2. `test_stack_spec` asserted `FORMAT_HAS_RESULT is False`; Stack A's result flipped it the
   same cycle, so the test was rewritten to assert the *rule* (ceiling follows the flag)
   rather than today's value of the flag.
3. The runner loaded the full price file per call; batching years per call kept each
   device-shell call inside its ~3-minute limit (3 calls for 7 years).

## WHAT NEW INFORMATION THIS RUN PRODUCED
The expected-range regime known at the prior close does **not** change the forward outcome
of an opening-range breakout on NQ — a powered null, not a thin one. That is the first thing
the project knows about how its two bot components interact, and it says the Risk/State
Engine belongs where it already sits (size, stop, permission) and nowhere else.
