## Session 2026-09-10 04:15-04:4x UTC (automated) -- duration: ~25 min

First automated run of AGENT PROTOCOL v2.1 (frozen 2026-09-10 04:12, ~3 min
before this session's trigger fired). No prior automated session has run
this protocol end-to-end; this report is being written incrementally as
work completes, per the scheduled-task instruction to checkpoint every item.

### Agents

**LEARN -- RAN.** Consulted by name before any scope freeze, per Rule 5.
Reviewed the live ledger (143 rows), SCAN_REGISTRY (4 closed scans, 152
cells), and the state-primitives docstrings' own "deferred" lists, rather
than reading docs/BACKLOG.md end to end.

  Finding: (KNOWN TRUE) volatility/range clustering is a repeatedly-confirmed
  structural property of this market -- three independent framings
  (range contraction/expansion hyp-000046/048, overnight coil hyp-000056/057,
  midday-lull afternoon expansion hyp-000105/106) each passed a pre-registered
  Validation-slice test in its raw form. (KNOWN FAILED) level_sweep_reversal
  family (multiple variants), vxn_level_standalone_signal (hyp-000029),
  vxn_roc_standalone_signal (hyp-000030), zn_bond_lead_standalone_signal
  (hyp-000031, Discovery-promising but its own Validation test hyp-000035
  already REJECTED it -- ledger still stale on this, see Moved section),
  zn_multiday_lead_standalone_signal (hyp-000034), and from the Discovery
  Engine specifically: zn_level_vs_trailing/mid/h10d, opening_range_vs_atr/
  mid/h1d (Scan 003, split-sample), zn_level_vs_trailing/low/h10d (Scan 003,
  regime-split), volume_vs_expected/low/10d, directional_persistence_quintile/
  q2/10d, vxn_level_vs_trailing/low/10d (Scan 002, split-sample flip).
  (KNOWN FAILURE MODES) (i) secular-regime split -- the Discovery window
  spans the 2018 selloff, the 2020 vol spike, and the 2020-21 melt-up, and at
  least two Scan 002 candidates (VXN-level, directional-persistence) flipped
  sign between chronological halves purely from this; (ii) volatility-regime
  concentration -- an effect significant on the full sample but living
  entirely in one vol-regime subset (Scan 003's last survivor, n=220 high-vol
  vs n=334 low-vol near zero); (iii) barely-clearing-cost-floor cells are
  fragile -- Scan 004's sole survivor cleared the ATR floor by ~10% then
  failed split-sample by a 4.3x magnitude ratio between halves; (iv) stale
  ledger status is a recurring pattern, not a one-time incident -- the
  2026-09-10 04:05 hygiene sweep corrected 9 rows, and this session's own
  LEARN check found a 10th (hyp-000031, detail below).
  (KNOWN UNEXPLORED, and the reason this matters for today's staff meeting)
  the "does instrument X's own level/state predict NQ" search space that the
  Research Director has been drawing Scan candidates from is close to
  exhausted: VWAP-distance (spent as H118, frozen), volume-vs-expected
  (killed), VXN level and ROC (killed twice), ZN level and lead (killed
  three separate ways). NEXT_UP.md's queued Scan 005 direction names exactly
  these four families as "genuinely new." They are not. Two axes remain
  genuinely untried: (a) short-horizon (intraday/overnight/1-2 day)
  re-expression of the FIVE OLDER descriptors that have only ever been
  scanned at daily close-to-close horizons (range_vs_atr, overnight_range_vs_atr,
  opening_range_vs_atr, location_in_range, directional_persistence) -- Scan 004
  tested short horizons but only for 3 brand-new descriptors, never revisited
  these five; (b) cross-instrument DIVERGENCE/relative-value state (NQ vs ZN,
  NQ vs VXN co-movement breaking down) as a genuinely different mechanism
  claim from "instrument X's own level predicts NQ," never tried in this
  project. Also: NEXT_UP.md asserts ES data is "already on disk" alongside
  VXN and ZN -- checked `data/`, this is factually wrong; only NQ, ZN, and
  VXN series exist. ES is not available without a new data pull.

  Confidence: HIGH (ledger + SCAN_REGISTRY + docstrings are primary sources,
  not narrative recall).
  Novelty: N/A (LEARN reports history, not a candidate).
  Research Value: HIGH -- directly changes what Scan 005 should scan.
  Recommendation: MODIFY the proposed Scan 005 direction before scoping it.

**Research Director -- RAN (entry checkpoint + convened staff meeting).**
Convened the mandatory methodology staff meeting (NEXT_UP queue item 0m,
Rule 3(c): Scans 001/003/004 zero-survivor, Scan 002 the project's only
survivor -- already-overdue trigger). Chaired below. Also performed the
entry-checkpoint function for Scan 005 scoping (see Staff meeting
disposition and Moved section).

  Finding: three of four closed Discovery Engine scans produced no
  survivor; the fourth produced the project's only fully-promoted candidate.
  Confidence: HIGH (SCAN_REGISTRY is authoritative).
  Novelty: N/A.
  Research Value: HIGH -- this is the standing question the whole session's
  direction depends on.
  Recommendation: see disposition below (MODIFY).

**Discovery -- RAN (position at staff meeting; no new scan run this
session pending the meeting's outcome).**

  Finding: the descriptor-bucket-scan + strict statistical-filter method is
  behaving as designed. ~152 grid cells across 4 scans plus roughly a dozen
  hand-built standalone tests have produced exactly one candidate that
  survived chronological Discovery, Validation, AND Holdout (H118) -- a hit
  rate that is not obviously inconsistent with a small number of cells
  carrying a real, small effect against a strict 90%-CI-above-zero bar. The
  zero-survivor scans are the split-sample and regime-split screens correctly
  killing fragile or contaminated cells, not evidence the underlying method
  is broken.
  Confidence: MEDIUM (no formal false-discovery-rate estimate has been
  computed for the engine as a whole -- this is a qualitative read of the
  ledger, flagged as itself a KNOWN UNEXPLORED item: a proper FDR/power
  estimate for the Discovery Engine has never been done).
  Novelty: N/A.
  Research Value: MEDIUM.
  Recommendation: CONTINUE the method, but Discovery has no more genuinely
  fresh single-instrument own-state descriptors left to propose from the
  current menu -- it needs a new INPUT (short-horizon re-expression of old
  descriptors, or cross-instrument divergence framings, per LEARN) before a
  5th scan can be honestly called novel.

**Mechanism -- RAN (position at staff meeting).**

  Finding: Mechanism has never run prospectively in this project -- every
  mechanism artifact to date (H118's, and the three being written this
  session) was written post-hoc, after the candidate already had Discovery
  and Validation numbers in hand. That is a real weakness (a post-hoc story
  earns nothing by sounding good), but it also means the project has not yet
  approved a candidate on a mechanism-free basis -- the MECHANISM GATE is
  functioning as a real filter now that it exists, even applied
  retroactively.
  Confidence: MEDIUM.
  Novelty: N/A.
  Research Value: MEDIUM -- process observation, not a scan-direction input.
  Recommendation: MODIFY going forward -- a candidate should get at least a
  brief pre-registered mechanism sketch BEFORE Discovery is run on it, not
  only after a number exists. Does not bear directly on the Scan 005
  question either way.

**Statistical -- RAN (position at staff meeting, using
`src/project_wide_multiplicity.py` re-run this session).**

  Finding: applying the current project-wide Sidak correction (N_validation=18,
  N_discovery=238 including Scan 004) to the three candidates now sitting at
  Validation shows the headline Validation passes are considerably weaker
  than the raw numbers quoted in the ledger: range-contraction narrow
  (single90 0.888-0.998, adjusted 0.852-1.036 -- FAILS, crosses 1.0),
  range-contraction wide (adjusted 0.978-1.184 -- FAILS), overnight-coil
  (adjusted 0.786-1.003 -- FAILS, barely crosses 1.0), midday-lull narrow
  (adjusted 0.718-0.914 -- SURVIVES), midday-lull not-narrow (adjusted
  0.999-1.183 -- FAILS, barely crosses 1.0). Of five tested Validation legs
  across the three candidates, only ONE survives correction.
  Confidence: HIGH (direct tool output, not estimated).
  Novelty: N/A.
  Research Value: HIGH -- directly relevant to whether pipeline effort this
  session should go to a new scan or to the candidates already in flight.
  Recommendation: MODIFY -- do not scope a 5th grid scan (which only grows
  N_discovery and makes the correction stricter for every candidate still in
  flight) before the three pending candidates have had their own Statistical-
  stage pass against these corrected numbers.

**Integrity Gate -- RAN (position at staff meeting; VETO authority per
Rule 3 applies inside the meeting).**

  Finding: agrees zero-survivor is the correct trigger for this meeting, and
  adds the sharpest objection: of the variable families floated in NEXT_UP.md
  for Scan 005 (VWAP distance, volume-vs-expected, cross-market
  ES/VXN/ZN), four are not new -- VWAP-distance is already spent as H118 and
  frozen; volume-vs-expected, VXN-level, and ZN-level/lead framings are each
  already closed-dead. Re-scanning any of them, even at new horizons, engages
  the 2-attempt limit and requires a logged per-variable resurrection ruling
  BEFORE a scoping doc is written, which has not happened. Also flags the
  ES-data claim in NEXT_UP.md as a factual error (not on disk) that would
  have wasted a scan cycle if not caught before scoping.
  Confidence: HIGH.
  Novelty: N/A.
  Research Value: HIGH.
  Recommendation: no veto on continuing the method itself. Hard requirement,
  binding if Scan 005 is scoped: it may not include vwap_dist_vs_atr,
  volume_vs_expected, vxn_level_vs_trailing, or any zn "own-level"/"lead"
  framing without an explicit, logged resurrection ruling per variable, and
  it may not reference ES data.

### Staff meeting

Convened. Trigger: (c), zero-survivor scan (Scan 004) plus the standing
"3 of 4 scans zero-survivor" condition -- already flagged overdue in
NEXT_UP.md before this session started.

Positions: recorded above, one per agent (Research Director, Discovery,
Mechanism, Statistical, Integrity Gate; LEARN's finding fed all of them).

**Disposition: MODIFY.**

The method (descriptor-bucket Discovery scanning under the existing
statistical filter) is NOT abandoned or paused -- it produced the project's
only fully-promoted candidate and the zero-survivor scans reflect the filter
correctly rejecting fragile/contaminated cells, not the method failing. What
changes:

  1. Scan 005 may NOT rescan vwap_dist_vs_atr, volume_vs_expected,
     vxn_level_vs_trailing, or any zn "own-level"/"lead" framing without an
     explicit, logged Integrity Gate resurrection ruling per variable
     (Integrity Gate's binding requirement, unopposed).
  2. If scoped, Scan 005 should draw from one or both of LEARN's two
     genuinely untried axes: short-horizon (intraday/overnight/1-2 day)
     re-expression of the five OLDER descriptors never tested below a daily
     close-to-close horizon (range_vs_atr, overnight_range_vs_atr,
     opening_range_vs_atr, location_in_range, directional_persistence), or
     cross-instrument divergence/relative-value state (NQ-ZN, NQ-VXN
     co-movement breakdown) rather than any single instrument's own level.
  3. NEXT_UP.md's claim that ES data is already on disk is corrected (it is
     not) -- see Moved section.
  4. Before Scan 005's scope is frozen in a future session, the three
     pending Validation-stage candidates (hyp-000048, hyp-000057,
     hyp-000106) should get a Statistical-stage pass against the corrected
     numbers above, per Statistical's recommendation -- queued ahead of
     Scan 005 scoping.

Given the volume of blocking work already queued (Mechanism docs, item 0n)
and the Statistical-stage follow-up this meeting itself surfaced, Scan 005
scoping is NOT attempted in this session -- queued next, under the corrected
direction above, rather than rushed to fit the remaining window.

### Ledger hygiene (found via LEARN's resurrection check, fixed in place)

hyp-000031 (zn_bond_lead_standalone_signal) still read PROMISING despite its
own required prospective test (hyp-000035) already failing on the Validation
slice on 2026-09-07. A 2026-09-09 correction attempt (hyp-000100) diagnosed
this correctly but spent a new hypothesis id instead of calling
`update_status` on hyp-000031 itself -- the exact anti-pattern the 04:05
session flagged for hyp-000091/93/94/95/96. Fixed the same way that session
fixed those: `research_ledger.update_status('hyp-000031', 'REJECTED', ...)`,
same id, append-only, no new id spent. Backup before the write:
research/ledger/hypotheses.jsonl.bak-20260910-0440. `get_latest('hyp-000031')`
confirmed REJECTED afterward. 144 ledger rows now (was 143).

### Mechanism doc 1/3 -- Range Contraction/Expansion Cycle (hyp-000046/048)

Written: research/mechanisms/range-contraction-expansion-cycle.md.
POST-HOC (results already known). Two competing claims stated: (a) a real
coiling-ahead-of-catalyst / release-after-shock volatility cycle, vs (b) a
mechanical regression-to-the-mean artifact of normalizing today's range
against its own trailing average. 3 testable predictions (lag decay,
calendar/OPEX clustering, VXN coherence), all UNTESTED -- queued as
follow-up work, not run this session. Also folds in this session's
project-wide-multiplicity re-run: BOTH Validation legs fail the corrected
90% bound (narrow adj 0.852-1.036, wide adj 0.978-1.184), even though the
raw ledger CIs were credible -- flagged as material to the next Statistical
pass, independent of the mechanism question.

  Finding: mechanism doc written; genuinely undetermined whether this is
  real market behavior or a normalization artifact; corrected Validation
  numbers are weaker than raw.
  Confidence: MEDIUM (predictions untested; multiplicity math is HIGH
  confidence, direct tool output).
  Novelty: N/A (post-hoc).
  Research Value: MEDIUM-HIGH -- P1-P3 are cheap to test against data
  already on disk and would meaningfully sharpen or kill this candidate.
  Recommendation: CONTINUE to Statistical-stage pass using corrected
  numbers; test P1-P3 before any further pipeline spend.

### Mechanism doc 2/3 -- Overnight Coil -> Same-Day RTH Range (hyp-000056/057)

Written: research/mechanisms/overnight-coil-rth-range.md. POST-HOC. Claim:
same-day persistence of a "no catalyst overnight -> nothing to react to
during RTH" latent state -- explicitly distinguished from the range-
contraction candidate's opposite-direction reversion story (quiet ->
expansion one day later), since both are superficially "volatility
persistence" but are different claims about different clocks. 3 testable
predictions (RTH volume co-movement, calendar DE-clustering -- opposite
direction from the other doc's calendar prediction, VXN-regime robustness),
all UNTESTED, cheap, reusable existing descriptors/machinery. Multiplicity
re-run: Validation leg fails the corrected 90% bound (adj 0.786-1.003)
despite a credible raw CI.

  Finding: mechanism doc written, distinguished from a superficially
  similar candidate; corrected Validation number weaker than raw.
  Confidence: MEDIUM (predictions untested).
  Novelty: N/A (post-hoc).
  Research Value: MEDIUM-HIGH -- P3 in particular reuses existing
  Scan-003 regime-split code, essentially free to run.
  Recommendation: CONTINUE to Statistical-stage pass using corrected
  numbers; run P1-P3 as part of it.

### Mechanism doc 3/3 -- Midday Lull -> Afternoon Range Persistence (hyp-000105/106, OBS-FINDING-011)

Written: research/mechanisms/midday-lull-afternoon-persistence.md.
POST-HOC current story, but notably the ORIGINAL pre-registered mechanism
(pent-up energy -> active afternoon) was tested and REFUTED before this
document existed -- recorded honestly as a real falsification rather than
silently replaced. New claim: same-day within-session activity persistence,
same family as the overnight-coil finding but finer-grained and stronger.
4 predictions: P1 already refuted (recorded), P2 opening->midday
persistence (untested), P3 overlap with the overnight-coil state --
directly feeds the Portfolio Agent's standing double-counting concern
(untested, higher priority than P2), P4 effect-size-scales-with-window-
proximity ordering across all 3 candidates (confirmed post-hoc, marked as
weaker evidence since noticed after the fact). Multiplicity re-run: SPLIT
result -- narrow-midday leg is the only Validation leg across all three
candidates in this session's docs to survive the corrected 90% bound
(adj 0.718-0.914); not-narrow leg fails (adj 0.999-1.183).

  Finding: mechanism doc written; one genuine pre-registered refutation on
  record; one Validation leg survives correction, the only one of five
  checked this session.
  Confidence: MEDIUM-HIGH (P1 and P4's ranking are checked; P2/P3 untested).
  Novelty: N/A (post-hoc for the current story).
  Research Value: HIGH -- P3 bears directly on Portfolio's future
  incremental-information question across all 3 range-persistence findings.
  Recommendation: CONTINUE to Statistical-stage pass on the narrow-midday
  leg specifically; run P3 before or alongside it.

### Mechanism Gate -- summary after this session

All 3 candidates previously blocked at Validation for lack of a mechanism
document (NEXT_UP queue item 0n) now have one. None is fully resolved --
each has untested predictions queued as cheap follow-up work using
descriptors/machinery already on disk. No ledger status changed by writing
these docs (per protocol, Mechanism does not itself promote or kill).

### Remaining agents -- NOT RUN this session, input condition not met

**Discovery -- NOT RUN.** No scope was frozen this session (staff meeting
disposition explicitly deferred Scan 005 scoping to a future session,
under the corrected direction).

**Candidate Triage -- NOT RUN / not applicable to this session's moved
candidates.** Considered explicitly: does Triage's absence for hyp-000048/
057/106 (which now have Mechanism docs) count as a skipped stage? No --
Triage's input condition is "a scan produced >=1 cell clearing its gate."
These 3 candidates came from individually hand-built Observatory/batch
studies (exp-074/084/125), not a Discovery-Engine grid scan, so Triage's
input condition was never applicable to them (distinct from Scan 001/002's
grid candidates, which DID go through Triage historically). Not a gap;
see Returns section.

**Statistical -- NOT RUN.** Input condition (mechanism doc exists) became
true only as a RESULT of this session's own Mechanism work, for all 3
candidates simultaneously, near the end of the session. No regime-split or
split-sample robustness code has ever been written for these 3 candidates
(checked: no matching script in src/). Building and running that properly
for 3 candidates is real, non-trivial analysis work, not a synthesis of
existing numbers -- queued as the top-of-queue item for the next session
rather than rushed here. The multiplicity-adjusted numbers already computed
this session (in each Mechanism doc) are a running start on it.

**Director Re-Evaluation -- NOT RUN.** No candidate survived Statistical
this session (Statistical itself did not run).

**Monetization -- NOT RUN.** Director Re-Evaluation did not return CONTINUE
on anything this session.

**Integrity Gate (checkpoint) -- NOT RUN.** No candidate is about to cross
Discovery->Validation or Validation->Holdout this session.

**Portfolio -- NOT RUN.** Dormant; no candidate has cleared the full
promotion bar this session (0 validated directional strategies besides
H118, which is out of automated scope).

**Item 0 (EXECUTION CLOCK) -- NOT TOUCHED, per SCOPE BOUNDARY.** Directly
concerns H118 (promoted, frozen). Automated sessions flag and stop, never
act. Nothing to flag this session -- left completely untouched, consistent
with the prior session's handling.

### Returns issued

None. Considered one candidate: whether the 3 Validation-stage candidates
skipped Candidate Triage before Mechanism. Concluded no -- Triage's input
condition (scan-produced cell) was never applicable to these hand-built-
study candidates; see the NOT RUN note above. No stage was skipped in the
wrong order for anything that moved this session.

### Moved this session

- Staff meeting held (queue item 0m, was overdue): disposition MODIFY.
  Scan 005's proposed direction corrected (see Staff meeting section) and
  Statistical-stage work on 3 pending candidates prioritized ahead of it.
- 3 mechanism documents written (queue item 0n, was blocking):
  research/mechanisms/range-contraction-expansion-cycle.md,
  research/mechanisms/overnight-coil-rth-range.md,
  research/mechanisms/midday-lull-afternoon-persistence.md. Mechanism Gate
  now satisfied for every candidate above Discovery except H118 (already
  done) -- no candidate above Discovery now lacks a mechanism document.
- `src/project_wide_multiplicity.py` re-run (no code change) to get current
  numbers for the staff meeting and all 3 mechanism docs; confirms
  SCAN_REGISTRY already includes Scan 004 (N_discovery=238).
- Ledger hygiene: hyp-000031 (zn_bond_lead_standalone_signal) corrected
  in place, PROMISING -> REJECTED, via `research_ledger.update_status`
  (same id, no new id spent). Backup:
  research/ledger/hypotheses.jsonl.bak-20260910-0440. 144 ledger rows (was
  143). H118 and EXP-047 rows: untouched.
- Full test suite: 202 passed, 0 failed (pytest + purgedcv installed fresh
  this session, same as a prior session noted needing).
- No hypothesis_id spent this session. No new candidate promoted or
  killed. No spend. No Holdout slot touched. H118/EXP047 untouched.

### Queued next

See NEXT_UP.md rewrite (this session) for the authoritative queue. Top
items handed off: (1) Statistical-stage pass (4 questions + selection
disclosure) on hyp-000048, hyp-000057, hyp-000106 using this session's
multiplicity-corrected numbers as a starting point, plus each mechanism
doc's untested predictions (esp. hyp-000106's P3, overlap with the
overnight-coil state -- feeds Portfolio's future double-counting question);
(2) Scan 005 scoping under the corrected direction from this session's
staff meeting (short-horizon re-expression of 5 older descriptors, or
cross-instrument divergence framings -- NOT the VWAP/volume/VXN/ZN
resurrection risk NEXT_UP previously proposed, and NOT referencing ES data,
which is not on disk); (3) the QUEUED vwap_dist LOW 1-day variant (Integrity
Gate pre-clearance, item 1a) -- not started this session, still queued as
Jason set it up 2026-09-10 02:20; (4) item 0 (EXECUTION CLOCK) remains
Jason's / out of automated scope, untouched.

