# Session — September 15th, ~10:00–11:00 pm CT (2026-09-15 ~03:00 UTC; interactive: Jason's refocus memo)

Interactive work on Jason's standing direction of September 15th
(research/infrastructure/refocus-2026-09-15.md, verbatim copy of his memo). It
supersedes the September 14th "push this as fast as possible" acceleration.
Scheduling (the 1/3/5/7 am triggers deleted, the 9 am–11 pm CT day restored)
was handled outside this session; everything below is the memo's other seven
sections, done, committed and pushed. No research was run. Every claim below
cites `path:line` per the new reporting rule (memo 7.4a).

## What changed

**Direction recorded (memo 1–4, 6, 8).** `research/NEXT_UP.md:44-88` is the new
binding REFOCUS section: no profit deadline (rule that never bends), daytime
2-hour cycles with the 24-hour cadence marked SUPERSEDED
(`research/NEXT_UP.md:1554-1556`), magnitude research FROZEN
(`research/NEXT_UP.md:309-313`), queue 0-ACCEL superseded and
`src/batch_screen.py` not to be run again (`research/NEXT_UP.md:649-652`), a
cycle with no real queue item closes early, Full Scope weekly or on request.

**Directional lane opened (memo 4).** Queue item 0-DIR at the top of the queue
(`research/NEXT_UP.md:616-641`): own slot count, non-state sourcing only, hard
stop at 20, count kept in `research/ledger/directional_lane.json`
(`{"cap": 20, "used": 0, "trials": []}`). No trial was run; the next cycle owns
trial 1 through the Mechanism-first pipeline.
**Discrepancy found and recorded, not corrected in his memo:** the memo says the
overnight-versus-intraday split "has never been tested as a hypothesis, only as
an integrity check." The ledger says it was: hyp-000145
(`research/ledger/hypotheses.jsonl:178`), Entry 19 / M15
(`research/idea_inventory.md:1348`), Scan 020, September 12th, Lou-Polk-Skouras
"night leg carries the drift" form — night-minus-day +0.006 ATR, block CI
(-0.028, +0.038), clean null, attempt 1 of 2. So trial 1 is either attempt 2 of
that family under a genuinely different claim, or the next non-state
directional idea (Entry 32 / M28 pre-holiday effect is sourced and drawable).
That call belongs to the cycle that owns trial 1, and to Jason.

**Shelf.** Entries 37–61 (the 25 batch-screen survivors) marked PARKED in
`research/idea_inventory.md:2010-2514` per SHELF RULE v2; Entries 33/35/36
(sourced magnitude ideas) PARKED under the same freeze. `src/cycle_preflight.py
--skip-lock --skip-budget` now reads "Shelf 1/3 DRAWABLE (Entry 32 ...) ->
SOURCING OWED" — the survivors no longer count and the "oldest drawable" line
no longer points at them.

**Weekly verification cycle (memo 7.2).** Queue item 1-VERIFY
(`research/NEXT_UP.md:642-648`) and the checklist
`research/infrastructure/weekly-verification-checklist.md`. Jason books it.

**Reporting rule (memo 7.4a).** SESSION REPORT FORMAT gained CITE FILE AND LINE
and ACTUAL USAGE (`research/NEXT_UP.md:1537-1542`).

**Console retired (memo 5).** `git rm`: `src/generate_console_state.py`,
`research/_console_state.json`, `tests/test_generate_console_state.py`; the
untracked `_console_prev_v87.json` moved aside. `src/ops_checks.py` lost the
console freshness check (`src/ops_checks.py:46-47`), `research/CYCLE_PROMPT.md:45-47`
and the NEXT_UP push-paths block (`research/NEXT_UP.md:933-934`) say retired.
Dated session reports untouched. Nothing rebuilt.

**Busy-work alarm (memo 7.1).** `src/cycle_budget.py:17-26` (docstring),
`movement()` (`src/cycle_budget.py:115-128`): a cycle MOVED only if a candidate
changed stage in the ledger (latest-row status/slice changed, or a new
hypothesis), a scan was registered, or a bot milestone flipped. Shelf/inventory
additions, studies and mechanism docs count zero. Regression tests
`tests/test_cycle_budget_and_value.py` (inventory-only cycle is NOT movement;
stage change IS; three inventory-only cycles = BUSY WORK FAIL).

**Actual minutes (memo 6).** `src/session_report.py` OPERATIONS now prints this
cycle's minutes from the budget clock (`src/session_report.py:271-291`) and
today's cycles run + minutes from `research/_cycle_compliance.jsonl`
(`src/session_report.py:294-314`). `tests/test_session_report.py` (3).

**Production write guard (memo 7.3).** `src/production_paths.py`: default-deny;
`TONY_PRODUCTION=1` is the only door, set by each standing entry point's
`__main__` block (never at import) or by hand for one-off scripts. 18 guarded
call sites in 13 scripts; `tests/conftest.py:46-62` strips the flag for every
test; `cycle_close.py`'s pytest child never inherits it. Audit:
`research/integrity/write-path-audit-2026-09-15.md` — 13 files + 2 trees + 1
glob PROTECTED; ~150 per-run/scratch writers NOT protected, each with a reason.
Real check: `python3 src/bot_stack_paper_run.py --strategy dummy` ran normally,
"0 new row(s) appended", log unchanged at 22 rows, no guard error.
`tests/test_production_paths.py` (8) proves the real ledger, execution log and
journal are refused at their real paths with the conftest redirect undone.

**GitHub (memo 7.5).** Verified after the final push; result in the log line
below and in the report to Jason.

## Tests
`python3 -m pytest -q`: **498 passed** (was 491; console tests removed, 18 added).
`pytest.ini` added so `_to_delete/` (files the device shell cannot unlink) is
never collected.

## Not done, on purpose
- No directional trial run (the next cycle's, through Mechanism-first).
- No Full Scope document touched (weekly now).
- `src/batch_screen.py` not run; `research/_batch_screen_state.json` untouched.
- Triggers untouched (handled outside this session).
- Jason's figures in the memo ("seven operational defects", "87 directional
  nulls", "~9,910 state combinations") recorded as his, not audited here.

## Commits
1. Refocus: record the standing direction; directional lane; magnitude freeze; parks
2. Retire the console
3. Busy-work alarm counts stage changes only; session report prints actual minutes
4. Production write guard + write-path audit
5. Session file, NEXT_UP log line, session log
