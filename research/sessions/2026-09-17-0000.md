# Session — September 16th, 7:00 pm CT scheduled cycle (2026-09-17 ~00:0x–00:4x UTC)

Standing process: research/infrastructure/standing-directive-2026-09-15.md (s.3, Steps 1–5)
**plus Jason's Amendment 1 and Amendment 2 of September 16th**, appended to the same file.
Last cycle: research/sessions/2026-09-16-2200.md (5:00 pm CT, S010 killed at screen, S010a spawned).

## Step 1 — Preflight
`python3 src/cycle_preflight.py --owner cycle --note "7:00 pm cycle"` clean
(research/_cycle_preflight.json): lock held, budget 105 min, pipeline sweep written, both daily
checkers ok, queue **S010a / S009a / S004 / S005 / S006 at SOURCE**, **paper book 5 strategies,
4 SLOW** — S001 1 trade / 9 days, S002 2 / 9, S003 0 / 9, S001a 0 / 9 (all SLOW), **S008 2 / 9,
NOT SLOW, 38 trades to judgment, six-week mark in 4.7 weeks**. Nothing at a judgment point;
nothing is judged under 40 trades (Amendment 1).

## Step 2 — Data check
`data/NQ_1min_databento_2026-09-16.csv` unchanged since the 5:00 pm cycle (253,055,842 bytes;
last bar **2026-09-16 00:50 ET**). The 7 am CT cron tops price data up once a day, so nothing is
due until tomorrow morning. `ops_checks` `data-currency`: price data through 2026-09-16 — no
s.10 interrupt (src/ops_checks.py, `[PASS] data-currency`).

## Step 3 — Advance the paper book
`TONY_PRODUCTION=1 python3 src/bot_stack_paper_run.py --strategy s001 / s001a / s002 / s003 /
s008 / dummy`: each reported **0 new rows across 0 sessions**, log total unchanged at **60 rows**.
**Nothing scored** — correctly, because there is no newly complete session, not because anything
failed.

**PAPER BOOK, unchanged this cycle, at the corrected costs** (`python3 src/paper_book.py`;
ASSUMED, MNQ market $2.60 = 1.300 pt, src/cost_model.py:1):

| strategy | SLOW? | trades | win | avg R (net) | 1 / 5 / 10 micros | to 40 |
|---|---|---|---|---|---|---|
| S001 level sweep reversal, compressed prior day | SLOW | 1 | 0% | −1.03R | −$93 / −$466 / −$931 | 39 |
| S001a same, prior-day levels only | SLOW | 0 | — | — | — | 40 |
| S002 overnight carry, compressed prior day | SLOW | 2 | 50% | −0.03R | −$52 / −$259 / −$518 | 38 |
| S003 month-end payment-cycle reversal | SLOW | 0 | — | — | — | 40 |
| **S008 late-day rebalance continuation** | no | 2 | 50% | −0.08R | −$114 / −$571 / −$1,142 | 38 |

All flagged **COST-FRAGILE** (avg R under 0.10 on assumed costs), all on samples that decide
nothing. Plumbing, never judged: `execution_dummy_4x_placeholder` 24 fills, `base_entry_b3` 0.

## Step 4 — THE REFERENCE-DATA COVERAGE GAP, fixed

**This is the cycle's stage advance.** It is not a study and not a mechanism doc: it unblocks
**two queued candidates** and repairs an input that every future Salvage check reads. Said
plainly rather than reported as "queue not advanced".

### What was actually wrong (research/ledger/data_coverage.json)

| series | covered through | short of the price data | consumers |
|---|---|---|---|
| **VXN** `data/VXNCLS_MAX.csv` | **2026-09-02** | 14 d | src/market_state_primitives_v2.py:60 `_load_vxn_daily` / :83 `extend_state_frame`; src/salvage_check.py:72 `vxn_labels` (**menu condition 1**); study_s002_night_leg_menu.py:66; idea_factory.py:66; observatory_free_look.py:47; the three `study_vxn_*`; the validated VXN → next-session-range fact used for stops/sizing; **S010a**, which selects LOW-VXN sessions only |
| **FOMC** frozen list, src/study_fomc_volatility.py:71 | **2021-09-22** | 1,820 d | src/salvage_check.py:100 `news_labels` (**menu condition 4**); study_fomc_volatility; study_pre_fomc_drift; **S009a**, which trades only on days with no scheduled macro event |
| **CPI** frozen list, src/study_economic_calendar.py:68 | **2023-12-12** | 1,009 d | as above + validate_exp039_economic_calendar |
| **NFP** same file | **2023-12-08** | 1,013 d | as above + study_pre_nfp_drift |

Enough for the Discovery screen. Not enough to score a 2026 session.

**And both failed OPEN, which is worse than failing at all.** `news_labels` read
`d in FOMC_SET` and called every miss QUIET — so a 2026 date was quiet because the list stops in
2021, not because the day held no release. `extend_state_frame` forward-filled the 2026-09-02
VXN close onto every session after it. Both read as data. Neither is. Failing open would have
made S009a's paper record S009's under another name.

### What was built

- **src/reference_data.py** — the single owner of coverage and the **fail-closed** accessors.
  `vxn_close()`, `scheduled_events_on()`, `is_scheduled_news_day()` raise
  `ReferenceDataUnavailable` for a session past coverage instead of returning a default
  (src/reference_data.py:99). The frozen sourced lists are **never edited** (s.13); a fetched
  extension is unioned with them, and with no extension coverage falls back to the frozen lists'
  own last date — conservative: it refuses more, never less. Macro coverage end is the
  **minimum** across FOMC/CPI/NFP, because a day is only honestly "quiet" once every series that
  could have put an event on it has been checked (src/reference_data.py:243).
- **src/data_topup_vxn.py** — incremental top-up from CBOE's published daily history,
  `https://cdn.cboe.com/api/global/us_indices/daily_prices/VXN_History.csv`, header
  `DATE,OPEN,HIGH,LOW,CLOSE` **verified live 2026-09-16**. Old observations win on every overlap;
  it **proves the fetched series is the one on disk** (trailing-40 reconciliation, abort on any
  disagreement — src/data_topup_vxn.py:100) before extending it; continuity-checks the result;
  logs what it added; production-guarded.
- **src/data_topup_macro_calendar.py** — incremental top-up of the FOMC/CPI/NFP schedule from
  federalreserve.gov and bls.gov, both of which publish ahead of time (that is the point: a 2026
  session can be classified honestly). **A mis-parsed schedule is a fabricated event date**, so
  the parser is assumed wrong until it proves otherwise: every parsed date inside a frozen list's
  range must reproduce that list exactly, and the result must have a real schedule's shape — 8
  FOMC decisions a year on a Tue/Wed/Thu, 12 BLS releases a year one to a month, NFP always a
  Friday. Any failure writes **nothing** and prints what it saw
  (src/data_topup_macro_calendar.py:195).
- **Fail-closed wiring in the consumers.** src/salvage_check.py:72 and :100 now refuse a session
  outside coverage instead of labelling it LOW/QUIET. src/market_state_primitives_v2.py:84 still
  forward-fills a hole *inside* the series and leaves the tail *past* the series end as NaN.
- **research/ledger/data_coverage.json** — each series' last date, its consumers and its updater,
  written by `python3 src/reference_data.py --write` and surfaced in
  `src/cycle_preflight.py:120 reference_data_status()`, so a future cycle sees staleness before
  it spends a candidate on it. Preflight prints it as a **warning, never a failure** — the
  consumers already refuse; what preflight owes the cycle is the warning.

### What could actually be fetched, and what needs Jason's Terminal

Verified by fetching, not assumed. `cdn.cboe.com`, `www.federalreserve.gov`, `www.bls.gov` and
`fred.stlouisfed.org` all answer **HTTP 403 at the proxy from the device shell and from the cloud
container** — the same wall `hist.databento.com` sits behind. Both updaters were run here and
both printed exactly that and wrote nothing. The URLs themselves are live and correct (confirmed
out-of-band: the CBOE CSV's header is `DATE,OPEN,HIGH,LOW,CLOSE`; the Fed's calendar page carries
2021–2027). **No volatility reading and no event date has been written into the repo from
anything other than a direct fetch by these scripts** — nothing was transcribed from memory.

So, exactly as with Databento, **Jason runs them**, at no cost, no account, no key:

```cron
5 7 * * *  cd ~/Documents/nq-research-platform-live && /usr/bin/python3 src/data_topup_vxn.py            >> data/_fetch_run.log 2>&1
10 7 * * 1 cd ~/Documents/nq-research-platform-live && /usr/bin/python3 src/data_topup_macro_calendar.py >> data/_fetch_run.log 2>&1
```

Write-up, with the dry-run instructions and the honest caveat that the macro-calendar parsers
have never met the live pages: research/infrastructure/reference-data-currency-2026-09-16.md.

### Why S010a was not also taken through SPECIFY this cycle

Its selection variable is the VXN series, which still ends 2026-09-02 until that cron runs.
Freezing a spec now would put a strategy in PAPER that refuses every live session — precisely the
objection recorded against S009a last cycle, and it would spend S010a's freeze on a record that
cannot accumulate. It keeps its queue position at SOURCE; the moment `data_topup_vxn.py` has run
once, both it and S009a are unblocked with no further work.

## Step 5 — Close-out
`python3 -m pytest -q`: **664 passed** (619 before — 45 added across tests/test_reference_data.py,
tests/test_data_topup_vxn.py, tests/test_data_topup_macro_calendar.py, and the fail-closed cases
appended to tests/test_salvage_check.py and tests/test_cycle_preflight.py). The load-bearing one
is `test_a_day_past_calendar_coverage_raises_instead_of_reading_quiet` — a session past coverage
is refused, not guessed. `ops_checks` WARNs are the pre-existing report-section and
awaiting-Jason items, not this cycle's. Commit 78f56b3, tree clean, origin/main == HEAD.

**Movement (s.3):** infrastructure that unblocks S009a and S010a and repairs Salvage menu
conditions 1 and 4. No paper trade recorded (no new data), no verdict due, no salvage owed.

## Agents
- **Operations Manager** — RAN. Steps 1, 2 and 5; owns the new `reference_data_status()` preflight row
  and the coverage ledger, and drew the line that a stale series warns rather than fails preflight.
- **Integrity Gate** — RAN. The binding call of the cycle: fail-closed over fail-open, and "a mis-parsed
  schedule is a fabricated event date" as the rule the calendar updater refuses to write against.
  Also held s.13: no frozen spec, no frozen sourced list and no paper fill was touched.
- **Statistical** — RAN. Named the blast radius of each stale series (Salvage menu conditions 1 and 4)
  and specified what a correct parse must look like before the parser was written.
- **Director** — RAN. Ruled this infrastructure IS the cycle's stage advance, and held S010a at SOURCE
  rather than freezing a spec whose paper leg would refuse every live session.
- **Discovery / Mechanism / Monetization** — NOT RUN. no new candidate was sourced or specified.
- **Bot (B7)** — RAN. Paper book advanced; nothing to score.

## WHAT NEW INFORMATION THIS RUN PRODUCED
Two of the project's four reference series had been silently failing OPEN, not merely running out:
`salvage_check.news_labels` called every post-2021 session QUIET because the sourced list ends
there, and `extend_state_frame` forward-filled the 2026-09-02 VXN close onto every later session.
Every Salvage check run since those series lapsed was therefore reading manufactured labels on any
date past coverage, which is a stronger finding than "the calendar is stale" and is what made this
a blocking repair rather than housekeeping. Separately: the sandbox egress wall is wider than
Databento — cboe.com, federalreserve.gov, bls.gov and fred.stlouisfed.org are all 403 from both
shells, so every free reference feed this project needs is a Jason's-Terminal job, not just the
paid one.
