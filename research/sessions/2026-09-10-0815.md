# Session report — 2026-09-10 08:15-~10:xx UTC (automated)

Third automated run of AGENT PROTOCOL v2.1. Worked NEXT_UP.md queue item
0 (Statistical-stage pass on hyp-000048, hyp-000057, hyp-000106) to
completion. Full numeric detail: research/studies/statistical-stage-pass-2026-09-10.md.
Mechanism docs updated in place (Prediction status / Verdict sections)
for all three candidates.

## Agents

**LEARN — RAN.** Consulted by name before re-testing any of the three
candidates, per Rule 5. Reviewed the live ledger (144 rows post-update),
SCAN_REGISTRY, the three mechanism docs, and src/project_wide_multiplicity.py's
hardcoded candidate table rather than re-deriving numbers from narrative.

  Finding: (KNOWN TRUE) all three candidates are established, real,
  well-replicated range/volatility-persistence effects at Discovery and
  raw Validation — nothing here is a resurrection of a previously-killed
  line (all three are moving FORWARD through the pipeline for the first
  time, not being re-tried after a prior kill). (KNOWN FAILURE MODE,
  directly relevant and pre-flagged before running anything) regime-
  concentration — the exact pattern that killed Scan 003's last
  survivor — was the single most important thing to check for on all
  three; this session's regime-dependence question (ATR-based) found
  none of the three concentrated, but the overnight-coil mechanism
  doc's own P3 (VXN-based regime split, a different cut) DID surface
  concentration. LEARN's standing advice, confirmed correct by this
  session's own result: one clean regime cut is not proof of robustness
  against every regime cut — check more than one variable before
  calling a candidate regime-clean. (KNOWN TRUE, methodological) the
  multiplicity-corrected CI, not the raw CI, is this project's actual
  bar as of 2026-09-09 — the mechanism docs already computed these
  corrected numbers; this session's job was the remaining 4 questions
  plus predictions, not re-deriving the correction itself. (KNOWN
  UNEXPLORED) no prior Statistical-stage pass in this project's history
  has run this exact 4-question-plus-predictions battery on a range-
  ratio (rather than point-return) candidate — no existing convention
  for "economically meaningful" on a ratio metric, so this session
  states and uses one explicitly (5% deviation from the 1.0 null,
  matching the existing ATR-normalized-effect floor's spirit) rather
  than silently assuming a number.

  Confidence: HIGH (ledger, mechanism docs, and project_wide_multiplicity.py
  are primary sources).
  Novelty: N/A (LEARN reports history, not a candidate).
  Research Value: HIGH — directly shaped which regime cuts to check and
  confirmed no resurrection risk before any code ran.
  Recommendation: CONTINUE with the queued Statistical-stage pass,
  check more than one regime variable per candidate (not just ATR).

**Research Director — RAN (entry checkpoint).** Framed the session:
queue item 0 is the top, pre-ordered item from the prior session's
staff meeting; no new scope to freeze (this is analysis of existing
frozen candidates, not a new scan), so the entry checkpoint's job here
is confirming that reading, not choosing a new direction.

  Finding: queue item 0's instructions are unambiguous and were
  followed as written — 4 questions + same-data selection disclosure,
  using the already-computed corrected numbers, plus each mechanism
  doc's queued cheap predictions, with an explicit standard for what
  advances (survive both the corrected numbers AND the mechanism
  checks).
  Confidence: HIGH.
  Novelty: N/A.
  Research Value: N/A (process checkpoint, not a finding).
  Recommendation: CONTINUE as scoped.

**Discovery — NOT RUN.** Input condition not met: no new scan or scope
was being frozen this session; all three candidates already have
Discovery data on record.

**Candidate Triage — NOT RUN.** Input condition not met: no scan
produced a new cell this session. (The 2 ledger status corrections
below are Statistical-stage dispositions recorded via update_status on
existing hypothesis_ids, not Triage grading of a fresh cell.)

**Mechanism — NOT RUN as a fresh pass; RAN as an UPDATE.** All three
candidates already had a mechanism doc (Mechanism Gate satisfied last
session) — this session's job was testing and recording each doc's
queued predictions, not writing a new doc. Treated as part of the
Statistical-stage work below rather than a separate agent invocation,
consistent with the Mechanism Gate's own statement that prediction
testing is follow-up work on an existing doc, not a new Mechanism pass.

**Statistical — RAN.** The full battery: stability (chronological
split-sample), regime dependence (ATR14-median split; VXN-median split
for overnight-coil per its own mechanism doc), magnitude (multiplicity-
corrected CIs, re-quoted from src/project_wide_multiplicity.py), and
selection sensitivity (pctl thresholds varied +/-5 around each frozen
cutoff), plus same-data selection disclosure, plus each doc's P1-P3
predictions. Full numbers: research/studies/statistical-stage-pass-2026-09-10.md.

  Finding: range-contraction and overnight-coil each pass all 4
  process questions (no stability, regime, or selection red flags) and
  gain real mechanism support (range-contraction's VXN-coherence P3
  especially), but both already fail the project's own multiplicity-
  corrected 90% bound at Validation — the binding constraint, unchanged
  by how clean the other checks are. Overnight-coil additionally
  surfaces a genuine (thin-sample) regime-concentration caution on its
  own P3 (VXN-based split) that the ATR-based regime-dependence
  question alone would have missed. Midday-lull's narrow leg is the
  only Validation leg of the five checked (across all three candidates)
  to survive the correction, and is also the only one to pass every
  process question AND gain two cleanly-confirmed new mechanism
  predictions (P2 opening->midday persistence; P3 partial, non-trivial
  overlap with overnight-coil, ~1.9x chance rate but not identity).
  Confidence: HIGH (n ranges 88-1751 across the individual checks;
  every check reused this project's existing bootstrap/CI conventions
  unmodified).
  Novelty: MEDIUM — first time this exact battery has been run on a
  range-ratio candidate in this project; the "economically meaningful"
  convention for ratio metrics (5% deviation from 1.0) is new and
  disclosed as a session-level choice, not an established standard.
  Research Value: HIGH — resolves the disposition of all 3 candidates
  queue item 0 named, with the corrected-CI standard doing the actual
  work (2 of 3 fail on that alone; the process questions mattered most
  for overnight-coil, where they added a second, independent reason to
  abandon).
  Recommendation: ABANDON range-contraction and overnight-coil (at
  Statistical stage, ledger updated in place). CONTINUE midday-lull
  narrow leg to Director Re-Evaluation.

**Director Re-Evaluation — RAN** (midday-lull narrow leg only — the
one candidate that survived Statistical). Second look: does this
evidence justify spending further pipeline effort, given everything
known?

  Finding: yes, with a clear-eyed view of what's actually been earned.
  This is a volatility/range CHARACTERIZATION, not a directional
  return-generating rule — Portfolio's activation bar (90% CI above
  zero on RETURNS, >=0.05R, confirmed on untouched data) does not apply
  to it directly and cannot be "passed" by this candidate as currently
  specified. What has been earned: the single cleanest, most-replicated,
  most mechanism-supported range-persistence finding in the project's
  history, already partially operationalized (src/volatility_conditioning.py's
  get_afternoon_conditioning(), built 2026-09-09 from hyp-000106).
  Further Statistical-stage spend on THIS same characterization (e.g. a
  5th or 6th regime cut) has rapidly diminishing value — the 4
  questions plus 2 predictions already asked most of what could be
  asked cheaply. The honest next step is not more characterization; it
  is translating the characterization into something with a costed,
  returns-based test, which is a Monetization question.
  Confidence: MEDIUM-HIGH.
  Novelty: N/A.
  Research Value: MEDIUM — the research question (is this real,
  stable, mechanism-supported) is now about as answered as it will get
  without new data or a new use case.
  Recommendation: CONTINUE to Monetization.

**Monetization — RAN** (midday-lull narrow leg). "No credible path" is
a valid success per the protocol — checked honestly rather than
assumed away.

  Finding: there IS a credible path, but it is a CONDITIONING path, not
  a standalone-signal path — consistent with every note already on
  hyp-000105/106/048/057's ledger rows. A range/volatility
  characterization cannot itself be scored against the return-based
  promotion bar; it needs to be wired into an existing setup's stop/
  target sizing or position sizing (the get_afternoon_conditioning()
  integration already does exactly this for the afternoon window) and
  THAT combined rule's economics tested, costed, against a baseline
  that does not use the conditioning. No such costed test exists yet
  for any of this project's three range-persistence findings. Two
  honest paths forward, both outside this session's remaining scope:
  (a) design and pre-register a costed conditioning-overlay study (an
  existing setup + get_afternoon_conditioning-style sizing vs. the same
  setup unconditioned) — in scope for a future automated session, no
  spend, no Holdout; (b) request a Holdout Gen2 slot to confirm the
  narrow-midday characterization itself on genuinely fresh data before
  investing in (a) — BLOCKED, needs Jason's sign-off per standing rule
  (Holdout slot decisions are never automated).
  Confidence: MEDIUM (path is credible and specific, but untested).
  Novelty: LOW — this is the same "needs a costed rule" conclusion
  hyp-000048's own ledger note reached 2026-09-08; today's contribution
  is confirming it's still the right read after re-testing everything
  else, not a new insight.
  Research Value: MEDIUM.
  Recommendation: CONTINUE — queue (a), the costed conditioning-overlay
  study design, as next-session work. Flag (b) for Jason rather than
  act on it (Holdout slot spend is his call, win or lose, per standing
  rule — not attempted this session).

**Integrity Gate (checkpoint) — NOT RUN.** Input condition not met: no
candidate crossed Discovery->Validation or Validation->Holdout this
session. Midday-lull's narrow leg stays at Validation (already crossed
that gate 2026-09-09); it was not sent toward Holdout (out of scope —
Holdout slot spend needs Jason).

**Integrity Gate (audit) — RAN.** See below.

**Portfolio — NOT RUN.** Input condition not met: no candidate cleared
the full promotion bar this session (range-persistence characterizations
cannot clear a returns-based bar without a costed rule, which doesn't
exist yet for any of the three).

## Returns

None. Every agent whose input condition was met this session ran; every
agent whose input condition was not met is listed above with the
specific unmet condition, not silently omitted.

## Staff meeting

Not convened. Checked all four triggers: (a) no candidate got a second
RETURN this session (none got even a first); (b) no two agents
returned conflicting recommendations on one candidate — Statistical,
Director Re-Eval, and Monetization were sequential and consistent, not
contradictory; (c) no scan closed this session (zero-survivor trigger
does not apply — no scan ran) and this is not the 3rd closed scan since
the last meeting (that meeting was this morning, 04:20 UTC, and closed
zero scans of its own); (d) no agent requested one.

## Moved

- research/mechanisms/range-contraction-expansion-cycle.md — Prediction
  status (P1/P2/P3) and Verdict sections updated with tested results;
  disposition ABANDON at Statistical stage recorded in the doc itself.
- research/mechanisms/overnight-coil-rth-range.md — same, disposition
  ABANDON at Statistical stage.
- research/mechanisms/midday-lull-afternoon-persistence.md — P2/P3
  tested and recorded (P1/P4 unchanged from before this session);
  disposition ADVANCES to Director Re-Evaluation recorded in the doc.
- research/studies/statistical-stage-pass-2026-09-10.md — new, full
  numeric writeup of the 4-question-plus-predictions battery on all
  three candidates.
- research/ledger/hypotheses.jsonl — hyp-000048 PROMISING -> REJECTED
  (in place, update_status, no new id spent); hyp-000057
  VALIDATION CANDIDATE -> REJECTED (in place, no new id spent).
  hyp-000106 (midday-lull Validation) status UNCHANGED (stays
  VALIDATION CANDIDATE — advancing to Director Re-Eval/Monetization is
  not a promotion, and no ledger status exists for "survived Statistical
  and Re-Eval and Monetization" short of the full promotion bar).
  Backup before edits: research/ledger/hypotheses.jsonl.bak-20260910-0815.
- research/_scratch_worksession/ — new scratch directory, two scripts
  (stage_a_build_cache.py, stage_b_statistical_pass.py) and their
  outputs (data/_worksession_cache/*.pkl, stage_b_results.json). Not
  promoted to src/ — see the study doc's Scope-and-method section for
  why (first use, not yet a recurring check). Left on disk for the
  next session to inspect rather than deleted (no delete permission in
  an automated session).

NEXT_UP.md queue item 0 is CLOSED. H118, EXP-047, and the EXECUTION
CLOCK item were not touched (out of automated scope). No spend, no
Holdout slot used, no candidate promoted, no live-capital action of
any kind.

## Queued next

1. Design and pre-register a costed conditioning-overlay study using
   the midday-lull narrow-midday characterization (Monetization path
   (a) above) — e.g. an existing detected setup's stop/target sizing
   conditioned on get_afternoon_conditioning()-style output vs. the
   same setup unconditioned, tested for a genuine (not just directional)
   economic difference. In scope, no spend, no Holdout.
2. Scan 005 scoping (queued by the 2026-09-10 04:20 staff meeting,
   direction already corrected there — short-horizon re-expression of
   the 5 older descriptors, or cross-instrument divergence framing; see
   NEXT_UP.md's own text for the frozen redirection). Now that item 0
   is closed, this is next per that meeting's disposition.
3. Flag for Jason (not acted on): a Holdout Gen2 slot for the
   midday-lull narrow-midday characterization would be the natural
   confirmatory step before investing further in item 1 above, but
   opening a Holdout slot is never automated — his call, win or lose.
4. Item 1 (EXECUTION CLOCK) and H118/EXP-047 remain untouched, out of
   automated scope, as every prior automated session has left them.
